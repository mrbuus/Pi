# Pi.mn — Кодын бүрэн аудит (2026-07-02)

Хамрах хүрээ: `api/` (NestJS + Prisma + PostgreSQL), `web/` (Next.js + Tailwind + KaTeX),
өгөгдлийн сан (2051 бодлого, 200 тест), импортын тайлангууд, SPEC.md-тэй харьцуулалт.

---

## 1. Гол асуултын хариу: зөв хариунууд ямар байрлалтай вэ?

Өгөгдлийн сангаас шууд шалгасан үр дүн (1916 CHOICE бодлого):

| Зөв хариу | Тоо |
|---|---|
| A | 397 |
| B | 357 |
| C | 372 |
| D | 388 |
| E | 366 |
| `{"manualReview": true}` | 36 |

**Дүгнэлт:** зөв хариунууд **нэг байранд бөөгнөрөөгүй, жигд тархсан** (A–E тус бүр ~19–21%).
Хариугүй/эргэлзээтэй 36 бодлого бүгд MANUAL горимын тестэд байгаа тул авто дүн бодогдохгүй — зөв тусгаарлагдсан.

Гэхдээ **байрлал бүх сурагчид ижил**: бодлогын дараалал ч, сонголтын дараалал ч
хэрэглэгч бүрд ижил үзэгдэнэ (shuffle байхгүй). Онлайн орчинд хуулах маш амархан. §5-г үз.

---

## 2. КРИТИК — контентын чанар (шалгалт ажиллагаанд шууд нөлөөтэй)

### 2.1 1913/1916 бодлогын `choices` нь placeholder
`Problem.choices` талбар 1913 бодлогод яг `["A","B","C","D","E"]` гэсэн үсэг л агуулж байна —
жинхэнэ сонголтын утга биш. Утга нь `statementText` дотор шигтгээтэй:

```
𝑠𝑖𝑛𝑥 = 1 тэгшитгэлийг бод. (𝑘 ∈ 𝑍)
 (𝐀). −0.5𝜋 + 2𝜋𝑘   (𝐁). 𝜋 + 2𝜋𝑘   (𝐂). 2𝜋𝑘 …
```

Үр дагавар: тест бодох UI ([tests/[id]/page.tsx](web/src/app/app/tests/[id]/page.tsx:398))
сонголт бүрийг товч болгож зурахдаа товчны текстэд «A», «B»… гэсэн үсгийг давхардуулж
харуулна («A) A», «B) B»). Сурагч сонголтоо бодлогын текстээс нүдээр хайж уншина —
утасны жижиг дэлгэц дээр маш эвгүй.

### 2.2 Зэрэг (илтгэгч) алдагдсан бодлогууд
PDF импортын үед superscript алдагдаж, жишээ нь `2^x = 8` бодлого DB-д
**«2 = 8 тэгшитгэлийг бод»** болж хадгалагджээ (100V2-EXPEQ-01-A-01).
`MathText.normalizeMath()` нь «латин үсэг + цифр» хэлбэрийг л нөхөж чаддаг (`x2 → x²`),
харин «тоо + алга болсон хувьсагч» тохиолдлыг сэргээж чадахгүй — **бодлого математикийн
хувьд утгагүй** харагдана. Сурагч ийм бодлогод зөв хариулах боломжгүй, тэр нь адаптив
аналитикт «алдсан» гэж бичигдэж дата бохирдоно.

**Шийдэл:** эх PDF/DOCX-оос математик бүтэцтэй (LaTeX) дахин импортлох, эсвэл
илтгэгч алдагдсан бодлогуудыг илрүүлэх аудит скрипт бичиж MANUAL руу шилжүүлэх.

### 2.3 Unicode math glyph-ууд (𝑠𝑖𝑛𝑥, 𝐀, 𝜋)
Текст нь Mathematical Alphanumeric Symbols блокоор хадгалагдсан — хайлт хийхэд
«sinx» гэж олдохгүй, фонтын fallback-аас хамаарч төхөөрөмж бүрт өөр харагдана,
screen reader уншиж чадахгүй. Импортын үед энгийн ASCII/LaTeX руу нормчлох хэрэгтэй.

---

## 3. КРИТИК — шалгалтын шударга байдал (exam integrity)

Жинхэнэ шалгалт өгдөг платформуудтай (ЭЕШ-ийн онлайн систем, Khan Academy,
TestPortal, Examity г.м.) харьцуулахад хамгийн том зөрүү нь: **сервер шалгалтын
төлөвийг огт мэддэггүй** — бүх хяналт клиент талд.

1. **Таймер зөвхөн клиент талд.** `Test.timeLimitMin` байгаа ч
   [tests.service.ts](api/src/tests/tests.service.ts:198) `submit()` хэзээ эхэлснийг
   мэдэхгүй, хугацаа хэтэрснийг шалгадаггүй. DevTools нээгээд `secondsLeft`-ийг
   өөрчлөх, эсвэл хүсснээрээ удаан бодоод илгээх бүрэн боломжтой.
2. **Refresh = таймер reset.** Хариултууд зөвхөн React state-д байдаг тул хуудсаа
   дахин ачаалахад бүх хариулт алга болж, intro дэлгэц рүү буцаад **таймер шинээр
   эхэлнэ**. Санамсаргүй refresh хийсэн сурагч бүх ажлаа алдана (UX осол),
   зориуд хийсэн сурагч цагаа сунгана (integrity осол). Autosave/resume байхгүй.
3. **Эхлэхээс өмнө бодлогуудыг урьдчилж харж болно.** `GET /tests/:id` нь
   «эхэлсэн» бичилт үүсгэдэггүй тул сурагч бүх бодлогыг нээж хараад, гадуур бодоод,
   дараа нь «Шалгалт эхлэх» дарж болно.
4. **Shuffle байхгүй.** Бодлогын болон сонголтын дараалал бүх сурагчид ижил.
   A/B хувилбарын бүтэц (`groupKey`/`variantLabel`) схемд байгаа ч онлайн тест дээр
   сурагчид хувилбар санамсаргүй онооддог логик алга.
5. **`timeSpentSec` хуурамч дата.** [page.tsx:153](web/src/app/app/tests/[id]/page.tsx:153)
   нийт хугацааг бодлогын тоонд хувааж бүх бодлогод ижил утга илгээдэг.
   Энэ бол адаптив алгоритмын (SPEC §9 — «гол давуу тал») түүхий эдийг эхнээс нь
   бохирдуулж байгаа хэрэг — бодлого бүрийн бодит хугацааг хэмжих ёстой.
6. **`occurredOn` UTC-ээр бичигдэнэ.** [tests.service.ts:231](api/src/tests/tests.service.ts:231)
   `new Date().toISOString().slice(0,10)` — УБ цагаар 00:00–07:59-д өгсөн тест
   **өмнөх өдрөөр** бүртгэгдэнэ. `attempts.service.ts` нь `Asia/Ulaanbaatar`-аар зөв
   бодож байгаа тул хоёр суваг хоорондоо зөрнө (өдрийн дүгнэлт, cron-д нөлөөлнө).
7. **MANUAL тестийг сурагч «Эхлэх» гэж нээж болно.** Бүгдийг бөглөсний дараа л
   «Багшийн дүн хүлээнэ» гэж хаагдана — хариултууд нь хаана ч хадгалагдахгүй, хөдөлмөр
   нь үрэгдэнэ. Жагсаалтад MANUAL тестийг ялгаж, огт эхлүүлэхгүй байх ёстой.
8. **Дүн гарсны дараа эргэн харах (review) байхгүй.** Сурагч аль бодлогод алдсанаа
   огт харахгүй — зөвхөн нийт оноо. `submit()` нь `graded` массив буцаадаг ч UI
   ашигладаггүй. Сургалтын платформын хувьд том дутагдал.
9. **Elo/correctRate хэзээ ч бодогддоггүй.** `Problem.eloRating` үргэлж 1000,
   `correctRate` үргэлж null — зөвхөн `attemptCount` нэмэгддэг. SPEC §9.3-ын 1-р үе
   (хамгийн наад захын статистик) хэрэгжээгүй. `Prediction` хүснэгт бүрэн хоосон хэрэглээтэй.

---

## 4. КРИТИК/ӨНДӨР — аюулгүй байдал

1. **Анхны нууц үг = утасны дугаар + rate limit байхгүй.** Утасны дугаар нь нэвтрэх
   нэр өөрөө тул нууц үгээ солиогүй бүх данс руу нэг оролдлогоор нэвтэрнэ.
   Дээр нь **нууц үг солих endpoint огт байхгүй** (SPEC §6.2: «анхны нэвтрэлтээр
   солих санал гарна» — хэрэгжээгүй). Throttler/lockout алга.
2. **JWT 7 хоног, refresh token байхгүй, revoke хийх аргагүй.** Role нь token дотор
   тул багшийн эрхийг хассан ч 7 хоног хүчинтэй үлдэнэ. localStorage-д хадгалдаг тул
   XSS гарвал шууд хулгайлагдана. SPEC §3 «JWT + refresh token» гэсэн.
3. **Файл upload-д төрлийн шүүлт байхгүй.** [uploads.controller.ts](api/src/storage/uploads.controller.ts)
   ямар ч өргөтгөлтэй файл авна; `GET /files/:key` нэвтрэлтгүй, өргөтгөлөөр нь
   Content-Type тавьж serve хийнэ → `.html`/`.svg` upload хийвэл API domain дээр
   stored XSS. Мөн даалгаврын (хувийн) зургууд түлхүүрийг нь мэдсэн хэн бүхэнд нээлттэй.
4. **QPay webhook баталгаажуулалтгүй.** `POST /payments/qpay/webhook` нэвтрэлтгүй,
   гарын үсэг (signature) шалгадаггүй, дүн тулгадаггүй; одоогоор invoice үүсгэдэг код
   байхгүй тул идэвхгүй ч, бүтэц нь аюултай суурьтай. Мөн webhook нь эрх (pass)
   олгодоггүй — SPEC §6.4 «төлбөр төлмөгц эрх шууд нээгдэнэ» урсгал ажиллахгүй.
5. **JWT_SECRET-ийн fallback код дотор бий** (`dev_only_change_in_production`) —
   production дээр env мартагдвал таамаглагдах нууцаар ажиллана. Секрет байхгүй бол
   унадаг (fail-fast) байх ёстой.
6. Идэвхжүүлэх код = өнөөдрийн огноо — SPEC ёсоор санаатай ч, огноог хэн ч мэднэ
   гэдгийг санах (хамгаалалт нь «ангид оруулах» гар алхам дээр л тогтоно).

---

## 5. Архитектур/логикийн цоорхой (SPEC-тэй зөрүү)

1. **Pass (эрх) систем тесттэй холбогдоогүй.** `getForTaking`/`submit` зөвхөн
   Enrollment(анги)-аар хандалт шалгана. Онлайн сурагч, худалдан авагч ангид
   бүртгэлгүй тул **эрх худалдаж авсан ч ямар ч тест өгч чадахгүй**. `Pass.scope`-ийн
   `testIds` талбарыг хаана ч уншдаггүй. (Контентын бүлэгт `chapterIds`/`bookIds`/`all`
   ажилладаг — [content.service.ts:195](api/src/content/content.service.ts:195), тест л орхигдсон.)
2. **Хоёр зэрэгцээ сонголтын загвар зөрчилддөг.** Хуучин: `Problem.choices` JSON +
   `correctAnswer` үсэг. Шинэ: `ProblemChoice` мөрүүд + `isCorrect` flag
   (schema-д тайлбартай, content.service үүсгэдэг). Гэвч:
   - `tests.service` дүн бодохдоо **зөвхөн** `correctAnswer`-ийг харьцуулна —
     `isCorrect` flag-ийг хэзээ ч ашигладаггүй;
   - шинэ загвараар үүсгэсэн бодлогод `choices = null`, `correctAnswer` = зөв сонголтын
     **текст** болдог. Тест UI `choices` байхгүй тул CHOICE бодлогыг **задгай текст
     оролт** болгож харуулна, сурагч «A» биш текст бичих ёстой болно — далд эвдрэл.
   - `getForTaking` нь `choiceOptions`-ийг select хийдэггүй тул шинэ загварын
     сонголтууд клиентэд огт очихгүй.
3. **Дахин өгөх бодлого байхгүй** — `TestResult` unique(testId, studentId) тул нэг л
   удаа. Сүлжээний алдаа/буруу дарсан тохиолдолд багш арчлах (устгах) API ч алга.
4. **OMR импорт (SPEC §8, §9.2-ын 4-р суваг) огт байхгүй.** Attempt-ийн
   `OMR_IMPORT` source тодорхойлогдсон ч ашигладаг код алга.
5. **Адаптив зөвлөмжийн хөдөлгүүр байхгүй** — «Миний сул талууд» (tag/chapter-ийн
   амжилтын хувь) л бий; «надад зориулсан бодлогууд», ЭЕШ онооны таамаг, сул
   сурагчийн горим (§9.4–9.5) байхгүй. (Үе шаттай төлөвлөгөөнд байгаа нь ойлгомжтой,
   гэхдээ Elo update шиг суурь нь одоо бичигдэх ёстой байсан.)
6. **Видео модуль** — схем бий, upload/үзэх урсгал алга. **Глобал хайлт** алга.
   **PWA** тохиргоо алга.
7. `enterResult` (36+4 цаасан дүн) нь бодлого бүрийн задаргаагүй, зөвхөн нийт оноо —
   SPEC §8.1 «дэд хэсэг бүрээр оноо тооцно» гэсэн; мөн Attempt үүсгэдэггүй тул
   3-р суваг (багшийн оруулсан шалгалт) адаптив датад орохгүй.
8. `myResults`-д эцэг эх хандах зам байхгүй (`@Roles(STUDENT)`) — parents модуль
   тусдаа шийдсэн эсэхийг нэгтгэж шалгах.

---

## 6. UX илрүүлэлтүүд

1. **Тестийн дундуур refresh/back → бүх хариулт устана**, анхааруулга (beforeunload) алга.
2. **NumericKeypad гар оролт дэмждэггүй** — компьютерээс өгч буй сурагч 36 бодлогод
   бүгдийг хулганаар дарна. Товчлуур том, mobile-д сайн; keyboard listener нэмэхэд амархан.
3. **Багш дүн оруулахдаа сурагчийн cuid ID гараар бичнэ**
   ([results/page.tsx:59](web/src/app/app/tests/[id]/results/page.tsx:59)) — ангийн
   сурагчдын dropdown/хайлт зайлшгүй хэрэгтэй. Одоогийн байдлаар бараг ашиглах боломжгүй.
4. **Login хуудас зөвхөн «Утасны дугаар»** гэсэн label-тай — backend нь имэйл/username
   -ээр ч нэвтрүүлдэг, хэрэглэгч мэдэхгүй.
5. Дүнгийн хуудсанд `maxScore = 0` үед `NaN%` гарна (хамгаалалтгүй хуваалт, 2 газар).
6. Тестийн жагсаалт pagination/хайлтгүй — 200 тест одоо байна, өсөхөөр удаашрана
   (багшид бүх тест `_count`-той nested query-гээр ачаалагддаг).
7. `tests/new`: `loadingProblems` хэзээ ч `true` болдоггүй (spinner ажиллахгүй);
   «36+4 загвар» товч зөвхөн утга бөглөдөг — 36 сонгох + 4 нөхөх бүтцийг үүсгэдэггүй.
8. Оройн тэмдэглэгээ (EveningMarking) — token-гүй, тест-суурьтай урсгал нь маш зөв
   шийдэл; optimistic UI зөв хийгдсэн. Гэхдээ 23:00 хаах цонх (SPEC §18.14) хэрэгжээгүй.
9. Alert/confirm-ийг inline card-аар шийдсэн нь сайн; theme сонголт (навы/цагаан) сайн.
10. `api()` сүлжээний алдаанд англи «Failed to fetch» үзүүлнэ — монгол fallback хэрэгтэй.

---

## 7. Жинхэнэ шалгалтын платформуудтай харьцуулсан дүгнэлт

**Pi.mn-д одоо байгаа давуу тал (ялгарах зүйлс):**
- 4 төлөвт өөрийн үнэлгээ (алдаагүй/зассан/алдсан/буудсан) — «азын оноог жинхэнэ
  чадамжаас ялгах» санаа нь Khan Academy/Duolingo-д ч байхгүй, жинхэнэ ялгарал.
- Оройн тэмдэглэгээний «утас хураадаг танхим»-д зориулсан урсгал — локал нөхцөлдөө
  тааруулсан өвөрмөц дизайн.
- Бодлогын гүн таксономи (арга/томьёо/алдах цэг) + answer-key статусын ил тод байдал
  (AUTO vs MANUAL) — олон платформ хариугүй бодлогоо нуудаг, энд шударгаар тусгаарладаг.
- Хариу харьцуулалт сайн: тоон каноник хэлбэр («4»=«4.0», «+5»=«5»), whitelist
  ValidationPipe, зөв хариу клиент рүү алдагддаггүй.

**Стандарт шалгалтын сайтуудад байдаг, энд байхгүй зүйлс:**

| Чадамж | Жишиг платформ | Pi.mn |
|---|---|---|
| Сервер-эрхт таймер, эхлэх бичилт | ✅ | ❌ клиент л |
| Autosave / тасалдсанаас үргэлжлүүлэх | ✅ | ❌ |
| Бодлого/сонголтын санамсаргүй дараалал | ✅ | ❌ |
| Хаагдсаны дараа алдсан бодлогын review + бодолт | ✅ | ❌ |
| Нээх/хаах хугацааны цонх (schedule) | ✅ | ❌ |
| Дахин өгөх бодлого (retake policy) | ✅ | ❌ |
| Tab-солих/fullscreen хяналт (proctoring-lite) | ✅ ихэнхэд | ❌ |
| Бодлого бүрийн хугацааны бодит хэмжилт | ✅ | ❌ (дундажлаад хуваадаг) |
| Accessibility (keyboard, ARIA) | ✅ | Хагас |
| ЭЕШ 36+4 multi-blank оноожуулалт (дэд хэсгээр) | ЭЕШ систем | ❌ (FILL нэг утга л) |

---

## 8. Засварын дараалал (санал)

**P0 — шалгалтын үнэн зөв ажиллагаа (одоо):**
1. `TestAttemptSession` (эхэлсэн цаг) серверт бичиж, submit дээр хугацаа + grace шалгах
2. Хариултын autosave (localStorage + сервер draft) — refresh-д тэсвэртэй болгох
3. `occurredOn`-ыг УБ цагаар (tests.service-д `todayUB()` хэрэглэх)
4. Бодлого бүрийн бодит `timeSpentSec` хэмжих (бодлого солигдох мөчид бүртгэх)
5. MANUAL тестийг сурагчийн жагсаалтад «Эхлэх»-гүй болгох
6. Дүнгийн дараах review дэлгэц (`graded`-ийг ашиглах) + NaN% хамгаалалт

**P1 — аюулгүй байдал:**
7. Нууц үг солих endpoint + анхны нэвтрэлтийн санал; login throttle (@nestjs/throttler)
8. Upload-д mime/өргөтгөлийн whitelist; хувийн файлд auth
9. JWT_SECRET fail-fast; refresh token
10. QPay webhook-т signature шалгалт (гэрээ хийгдэхэд)

**P2 — контент/дата чанар:**
11. Placeholder `choices`-ийг statementText-ээс салгаж жинхэнэ утгаар нөхөх
    (импортын parser сайжруулж дахин ажиллуулах); илтгэгч алдагдсан бодлогуудын аудит
12. Grading-ийг `ProblemChoice.isCorrect` руу нэгтгэх; `getForTaking`-д choiceOptions оруулах
13. Elo/correctRate-ийг submit бүрт шинэчлэх (§9.3-ын 1-р үеийн суурь)

**P3 — SPEC-ийн үлдсэн модулиуд:** pass↔тест холболт, сонголт/бодлогын shuffle,
retake бодлого, OMR импорт, адаптив зөвлөмжийн хуудас, глобал хайлт, PWA.
