# Pi.mn — төслийн БҮРЭН төлөв (2026-08-08)

> **Шинэ session эндээс эхэл.** Энэ файл нь төслийн одоогийн байдал, шийдвэрүүд,
> алдаанд хүргэсэн урхинуудыг агуулна. Үүнийг уншсаны дараа кодыг дахин
> судлах шаардлагагүй.
>
> Бусад баримт (`SPEC.md`, `DEPLOY.md`, `MASTER-PLAN-2026-07.md`,
> `HANDOVER-2026-07-27.md`, `TOIM-2026-07-29.md`, `AUDIT-2026-07-02.md`,
> `PERF-AUDIT.md`) нь ТҮҮХЭН — зөрчилдвөл **энэ файл давамгайлна**.

---

## 1. Юу вэ

**Шинэ Ирээдүйн Эзэд** сургалтын төвийн дотоод платформ.
ЭЕШ-ийн математик + нийгмийн ухааны бэлтгэл, танхим + онлайн.

- **Прод вэб:** https://web-one-pi-59.vercel.app
- **Прод API:** https://pimn-api.onrender.com/api
- **GitHub:** https://github.com/mrbuus/Pi (branch `main`)

### Хэмжээ (2026-08-08 хэмжсэн)

| | |
|---|---|
| Prisma загвар | 63 |
| Миграци | 26 |
| API маршрут | 206 |
| Вэб хуудас | 34 |
| React компонент | 181 |
| API unit тест | 226 (15 файл) — **бүгд өнгөрдөг** |
| Прод сурагч | ~359 |
| Прод анги | 21 |
| Хуваарийн мөр | 77 |
| Бодлого | ~6125 |

---

## 2. Локал ажиллуулах

```bash
# API (терминал 1)
cd /Users/mr.buus/Intern/Pi.mn/api && npm run start:dev     # :3000

# Вэб (терминал 2)
cd /Users/mr.buus/Intern/Pi.mn/web && npm run dev           # :3001
```

Шалгалт:
```bash
cd api && npx tsc --noEmit && npx jest      # 226/226 байх ёстой
cd web && npx tsc --noEmit && npm run build  # tsc ХАНГАЛТГҮЙ — build ЗААВАЛ
```

### Туршилтын бүртгэл (нууц үг = утасны дугаар)

| Эрх | Утас |
|---|---|
| ADMIN | `99294266` (Б. Өсөхбаяр) |
| TEACHER_PLUS | `80333020` (У. Эмүжин) |
| STUDENT | `80601209` (Гантөмөр Анар) |

> ⚠️ **Локал** ӨС-д админы нууц үг утастай таарахгүй байсан (2026-08-03).
> Продакшнд зөв. Локалд шалгах бол эхлээд bcrypt-ээр дахин тавина.

---

## 3. Эзний ХАТУУ дүрмүүд

Эдгээрийг зөрчвөл ажил хүчингүй. Бүгд бодит залруулгаас гарсан.

1. **Математик бичвэр ҮРГЭЛЖ LaTeX** (`MathText` компонент).
   `π`, `²`, `⅓`, `·` гэх мэт энгийн тэмдэгт **хэзээ ч** болохгүй.
   Эзний үг: "мaш хүндэтгэлгүй, муухай, заваан харагдаж байна".
2. **Emoji болон unicode глиф хориотой** — зөвхөн `lucide-react`.
   (⚠ ✓ ✕ ✎ → ← ↑ ↓ ↺ ⏱ ▲ ▼ ● бүгд солигдсон.)
3. **Нууц үг = утасны дугаар**, автоматаар. "Чанартай нууц үг" гэж
   санал болгохыг эзэн шууд татгалзсан.
4. **Жижиг тайлбар бичвэр олшруулахгүй** — "цэвэрхэн ухаалаг дизайныг
   хямд харагдуулдаг". Тайлбар хэрэгтэй бол `InfoHint` ("i" тэмдэг →
   дарахад гарч ирнэ).
5. **"Математик болон Нийгмийн ухаанд төвлөрсөн"** гэсэн үг хориотой.
   Оронд нь: "элсэлтийн шалгалтад бэлдэх + математикийн хоцрогдол
   арилгах танхимын сургалт".
6. **"Мэдээлэл авах" маягт** нүүр хуудсанд байхгүй (эзэн хассан).
7. **Интернэт удаан байхад UI муудах ёсгүй.** Видео/WebGL/гадаад фонт/
   CDN/зураг хориотой. Эффект бүр цэвэр CSS.
8. **Функц устгахгүй.** Дизайн сайжруулна гэдэг нь юм хасна гэсэн үг биш.
9. **Нэг зүйл хийгээд шууд push хийхгүй** — өдрийн төгсгөлд эсвэл
   эзэн хэлэхэд.
10. **Гарагийн дугаарлалт**: эзэн МОНГОЛ (1=Даваа…7=Ням), ӨС нь JS
    (0=Ням…6=Бямба). Андуурвал **бүх хуваарь нэг өдрөөр гулсана**.

---

## 4. Deploy — ЭНЭ НЬ ТЭГШ БУС

| | Автомат уу |
|---|---|
| **Render (API)** | ✅ `git push` дээр өөрөө deploy + `prisma migrate deploy` ажиллуулна |
| **Vercel (вэб)** | ❌ **ХИЙДЭГГҮЙ.** Гараар ажиллуулах ёстой |

```bash
cd /Users/mr.buus/Intern/Pi.mn/web && npx vercel@latest --prod --yes
```

Push хийгээд Vercel-ийг мартвал frontend хуучин хэвээр үлдэнэ. 2026-07-30-нд
яг ингэж 12 цаг хуучин хувилбар прод дээр байсан.

Deploy шалгах:
```bash
cd web && npx vercel@latest ls web --yes | sed -n '6,7p'
```

> ⚠️ Прод вэб дээр шалгахдаа **HTML-ээс grep хийж болохгүй** — client
> компонентийн текст эхний HTML-д байхгүй. Браузераар үзэх эсвэл
> `javascript_tool`-оор DOM шалгана.

---

## 5. Архитектур

```
Pi.mn/
├── api/                 NestJS + Prisma + PostgreSQL
│   ├── src/             31 модуль (auth, schedule, tests, users, …)
│   └── prisma/
│       ├── schema.prisma
│       ├── migrations/
│       ├── schedule-plan.cjs        ← хуваарийн ЦОРЫН ГАНЦ эх сурвалж
│       ├── seed-year-schedule.cjs   ← локал ӨС рүү шууд бичнэ
│       └── seed-prod-via-api.cjs    ← прод руу API-аар бичнэ
└── web/                 Next.js 15 App Router + Tailwind v4
    ├── DESIGN.md        ← дизайны гэрээ (шинэ UI бичихээс өмнө УНШ)
    └── src/
        ├── app/         34 хуудас
        ├── components/  181 компонент
        └── lib/api.ts   fetch + Render "сэрэх" retry
```

### Эрхийн загвар

`ADMIN` · `TEACHER_PLUS` · `TEACHER` · `STUDENT` · `PARENT` · `BUYER`

- Контентын эзэмшил **TEACHER_PLUS**-д (2026-07-27 шийдвэр)
- Мөнгө болон эрх солих нь **зөвхөн ADMIN**
- Энгийн `TEACHER` — өөрийн ангийн ирц/даалгавар, ерөнхий хуваарийг
  зөвхөн ХАРНА

### Дизайны систем (`web/src/app/globals.css` + `web/DESIGN.md`)

- 183 CSS хувьсагч, WCAG контраст тус бүрд хэмжсэн. **Өнгө гараар
  бичихгүй** — зөвхөн токен.
- Цайвар горим ӨГӨГДМӨЛ (судалгаагаар: жижиг математик тэмдэгт уншихад
  дээр). Харанхуй нь сонголт.
- Хөдөлгөөн: `--dur-micro/base/enter` (120/180/260ms), `--ease-out/spring`
- Гүн: `--elev-1/2/3` (харанхуйд гэрэлт ирмэг болж хувирна)
- Нягтрал: `--row-compact/base/roomy` (28/32/38px)
- `prefers-reduced-motion` үед бүх анимац унтарна

### Хуваалцах компонент (`web/src/components/ui/`)

| Файл | Юу |
|---|---|
| `StateBlock.tsx` | `LoadingState` / `ErrorState` (onRetry) / `EmptyState` |
| `Skeleton.tsx` | `SkeletonLine` / `SkeletonRows` / `SkeletonCard` |
| `Surface.tsx` | `Card` / `SectionHeader` / `PageHeader` |
| `ConnectionStatus.tsx` | Сүлжээ тасрахад доод тууз |
| `InfoHint.tsx` | "i" тэмдэг → дарахад тайлбар |

**Шинэ ачаалалт/алдаа/хоосон төлөв гараар бүү бич** — эдгээрийг ашигла.

---

## 6. Онцлох дэд системүүд

### 6.1 Хуваарь (хамгийн том, хамгийн сүүлд хийгдсэн)

**Хоёр өөр харагдац:**

1. **Ерөнхий хуваарь** (`PatternOverview.tsx`) — үндсэн таб.
   Босоо 2 бүс (Баруун 4 / Зүүн 4) × 4 давтамжийн багана
   (Да·Лх·Ба / Мя·Пү / Бямба / Ням). Багана доторх цагийн бүс автоматаар.
   Танхим **тогтмол эрэмбэтэй**: 501,502,503,504,403,404,405 → 301,302.
   *Яагаад 7 өдрийн багана биш: хуваарь 1,3,5-д яг ижил давтагддаг тул
   гурав давтахын оронд нэг удаа харуулбал бүтэн жил нэг дэлгэцэд багтана.*

2. **Долоо хоногийн төлөвлөгөө** (`WeekBuilder.tsx`) — огноот.
   Сэдэв зоох, зөөх, цуцлах.

**Гурван хамрах хүрээтэй засвар** (`EntryActionPanel.tsx`):
- Зөвхөн энэ өдөр → зөөх / цуцлах / сэдэв
- **Энэ ба цаашдын бүх** → `POST /schedule/:id/split` — цувралыг хоёр
  хуваана (хуучин мөр өмнөх өдөр дуусч, шинэ нь тэндээс эхэлнэ).
  *Яагаад PATCH биш: PATCH нь өнгөрсөн рүү ч үйлчилж, 3 сарын өмнөх
  ирцийн бүртгэл "өөр цагт болсон" мэт харагдана.* Сэдэв/онцгой
  тохиолдлыг шинэ мөр рүү зөөдөг.
- Бүх давтамж → устгах (улаан, 2 алхамт)

**Танхимын дүрс** (`RoomShape.tsx`) — хаалган дээрх бодит тэмдэг:
501=бөөрөнхий, 502=гурвалжин, 503=трапец, 504=квадрат, 403=ромбо,
404=таван өнцөгт, 405=тэгш өнцөгт (хамгийн жижиг тул сүүлд), Онлайн=дэлгэц.
Өнгө = **анги**, дүрс = **танхим**.

**Хуваарийн өгөгдөл** (`api/prisma/schedule-plan.cjs`), бүх хичээл 120 мин:

| Ангиуд | Давтамж |
|---|---|
| 12-1, 12-3, 12-5 | 1,3,5 @15:00 + Бямба @10:00 |
| 9-1, 11-1, 11-3 | 1,3,5 @15:00 |
| 12-7, 12-9, 12-11 | 1,3,5 @17:30 + Бямба @16:00 |
| 12-2, 12-4, 12-6 | 2,4 @15:00 + Бямба @13:00 + Ням @10:00 |
| 10-1, 11-2 | 2,4 @15:00 + Бямба @10:00 |
| 12-8, 12-10, 12-12 | 2,4 @17:30 + Бямба @16:00 + Ням @13:00 |
| 13-1 | 1,3,5 @15:00 + Бямба @10:00 |
| 13-2 | 2,4 @15:00 + Бямба @13:00 + Ням @10:00 |
| 10-2 | 2,4 @15:00 + Бямба @13:00 |
| НИЙГЭМ | 2,4,6 @17:30 |

Ангийн нэрийн засвар (хийгдсэн): `13-3 → 13-2`, `9-2 → 10-2` (анги 10).
Шинээр үүссэн: `12-6…12-12`, `НИЙГЭМ (Зүүн 4)`.

### 6.2 Нууц үг сэргээх (SMS)

`/forgot-password` — 2 алхам: утас → 6 оронтой код → шинэ нууц үг.

- **379 хэрэглэгчийн 0 нь имэйлтэй** тул имэйлээр сэргээх боломжгүй.
- Код HMAC-SHA256 (`RESET_TOKEN_SECRET`) — энгийн SHA-256 бол 6 оронтой
  код дээр 1 сая хувилбартай тул ӨС алдагдвал шууд эвдэгдэнэ.
- Хэрэглэгч байгаа эсэхийг **илчлэхгүй** — маскийг ӨС-ээс биш,
  хэрэглэгчийн бичсэн мөрөөс гаргана.
- 10 мин хугацаа, нэг удаагийн, 5 буруу оролдлогод үхнэ, IP+бүртгэлээр
  хоёр давхар хязгаар.
- `passwordChangedAt` + `JwtStrategy` — сэргээхэд **бүх сесс тасарна**.
- Ажилтан (Админ/Багш+) сурагчийн хуудаснаас код илгээж болно; **кодыг
  харахгүй**.

> ⚠️ **SMS нийлүүлэгч тохируулаагүй.** `SMS_PROVIDER` (console|twilio|http)
> орчны хувьсагч. Тохируулах хүртэл продакшнд `/auth/forgot-password`
> 503 буцааж илэн далангүй хэлнэ. Данс үүсгэх/төлбөр хийх нь **эзний
> ажил** — Claude хийж болохгүй. `render.yaml`-д хоёр хувилбарын жишээ бий.

### 6.3 Шалгалтын хөдөлгүүр

`TestAttemptSession` — сервер талын seed, `problemOrder`/`choiceOrder`
ӨС-д хөлдөөсөн, `mulberry32` PRNG. 1200мс debounce autosave, 20с±20%
heartbeat, `GRACE_MS=30_000`, `MAX_LEAVES=3`.

Дүгнэлтийн хоёр горим (`grading.ts`): `TEXT` (бүтэцтэй `ProblemChoice`,
холилдоно) vs `LETTER` (байрлал ХӨДӨЛӨХГҮЙ — сонголт нь статемент дотор).
`hasKnownAnswer()` хариугүй бодлогыг `maxScore`-оос хасна.

---

## 7. Цаг иддэг УРХИНУУД

Эдгээр нь бодит алдаанаас гарсан. Дахин бүү ал.

1. **`nest build` нь `dist/src/main.js` гаргадаг** (`dist/main.js` биш) —
   `prisma.config.ts` нь `src/`-ээс гадна байгаа учир. `start:prod` зөв.
2. **Render Prisma холболтод `?sslmode=require` заавал** — эс бөгөөс
   "User was denied access" гэнэ (psql ажиллаж байсан ч).
3. **Prisma JSON `null` ≠ SQL NULL** — `choices: null` нь JSON `null`
   бичдэг. `UPDATE ... WHERE choices::text='null'` хэрэгтэй болсон.
4. **`eslint --fix` нь хэрэгтэй type assertion устгадаг**
   (`tests.service.ts` `start()` дотор `as SessionRow`). tsc эвдэрнэ.
5. **`TestAccess` хүснэгтэд `id` багана байхгүй** — нийлмэл түлхүүртэй.
6. **`ActivityModule` import хийгдсэн ч `imports:` массивт байгаагүй** —
   бүх `/api/activity/*` 404 өгч байсан. **Статик шинжилгээ олоогүй,
   зөвхөн апп ажиллуулж байж олдсон.**
7. **`api/tsconfig.json`-д `ignoreDeprecations: "6.0"`** гэсэн хүчингүй
   утга орж, **10/10 тестийн багц бүхэлдээ уначихсан** байсан.
8. **Хөтчийн scroll**: `html { scroll-behavior: smooth }` тул
   `window.scrollTo(0,0)` шууд ажиллахгүй — `behavior:'instant'` өг.
9. **Агентууд "цэвэр" гэж хэлдэг ч биш** — 2026-08-03-нд 10 агент "бүх
   глиф сольсон" гэсэн атал 225 үлдсэн байв. Мөн дүрс нэмэхдээ
   мессежийн текстээс глифийг хасаагүйгээс **давхар харагдалт** үүссэн.
   **Агентын тайланг үргэлж өөрөө grep-ээр шалга.**
10. **Нэг файлыг хоёр агент зэрэг засвал** нэг нь нөгөөгөө дарна.
    Файлын эзэмшлийг агент бүрд ЯГ зааж өг.

---

## 8. Нээлттэй эрсдэл ба дараагийн ажил

| Асуудал | Хэн шийдэх |
|---|---|
| 🔴 **Render үнэгүй ӨС 2026-08-18-нд дуусна** — Basic-256mb руу шилжүүлэх | **Эзэн** (төлбөр) |
| 🔴 **SMS нийлүүлэгч сонгоогүй** — нууц үг сэргээх ажиллахгүй | **Эзэн** (данс+төлбөр) |
| 🟡 Render үнэгүй багц 15 мин ажилгүй бол унтдаг (эхний хүсэлт ~60с) | Эзэн (төлбөр) |
| 🟡 Диск байхгүй → байршуулсан зураг redeploy бүрт устана. S3 огт хэрэгжээгүй | Эзэн (багц) |
| 🟡 Хэрэглэгч устгах/архивлах боломжгүй (`User`-д `archived` талбар алга) | Код |
| 🟡 Сурагчдыг бөөнөөр импортлох UI байхгүй (скриптээр) | Код |
| 🟡 Эцэг эх зарлал харахгүй (`GET /announcements` зөвхөн STUDENT) | Код |
| 🟡 8 шалгалтын бодлого сэргээгдээгүй (эх PDF эвдэрсэн) | Код |
| ⚪ ~166 lint алдаа (ихэнх нь `react-hooks/set-state-in-effect`, CI-д унтраасан) | Код |
| ⚪ `studentCode`/`teacherCode` хоосон (эзний шийдвэрээр хойшлуулсан) | Эзэн |
| ⚪ `mustChangePassword` талбар бэлэн ч ИДЭВХГҮЙ (эзний шийдвэр) | Эзэн |

---

## 9. Ажлын хэв маяг (эзний хүсэл)

- **Олон subagent зэрэг ажиллуул** — гэхдээ файлын эзэмшил давхцуулж
  БОЛОХГҮЙ. Нэг файл = нэг эзэн.
- Subagent модель: `sonnet` (`~/.claude/settings.json` дотор
  `CLAUDE_CODE_SUBAGENT_MODEL`). Удирдлага/дүгнэлт нь өндөр моделиор.
- Эзэн монголоор бичдэг, монголоор хариул.
- Зөрчилтэй шаардлага ирвэл (жишээ нь "гоё эффект" + "интернэт удаан
  байхад нөлөөлөхгүй") **буулт хийхгүй, дизайнаар шийд** — тэгээд
  яагаад гэдгээ тайлбарла.
- Эзэн буруу мэдээлэл өгвөл (жишээ нь байхгүй ангийн нэр) **шууд хэлж
  тодруул**, таамаглаж болохгүй.

---

## 10. Түүхэн баримтууд

| Файл | Агуулга |
|---|---|
| `SPEC.md` | Анхны бүтээгдэхүүний тодорхойлолт |
| `DEPLOY.md` | Render/Vercel тохиргооны дэлгэрэнгүй |
| `MASTER-PLAN-2026-07.md` | 7 сарын төлөвлөгөө |
| `HANDOVER-2026-07-27.md` | Өмнөх гардуулалт |
| `TOIM-2026-07-29.md` | 10 агентын судалгааны дүгнэлт |
| `AUDIT-2026-07-02.md`, `PERF-AUDIT.md` | Аудит |
| `web/DESIGN.md` | **Дизайны гэрээ — UI бичихээс өмнө УНШ** |
| `web/AGENTS.md` | Next.js хувилбарын анхааруулга |

---

## 11. 2026-08-08 — томоохон сайжруулалт

Эзний даалгавар: «нэвтрэх/нууц үгийн хэсгийг маш ойлгомжтой болго, нууц үг
сэргээхийг дэлгэрэнгүй нэм; танхимын дүрсийг будалгүй тод болго; ангийн блок
будагдаж текст будагдахгүй байх; QPay-г бүх банкны апптай холбох; шалгалт
өгөх ба бодлого оруулах хэсгийг сайжруул; 20,000 хэрэглэгч, 2,000 зэрэг
шалгалт даах».

### 11.1 Хийгдсэн

**Нэвтрэх ба нууц үг** (`app/login`, `app/forgot-password`, `app/register`,
`components/profile/ChangePasswordSection.tsx`)
- «Нууц үг харах» товч бүх нууц үгийн талбарт. *Яагаад: анхны нууц үг = 8
  оронтой утасны дугаар. Од болгон нуусан 8 цифрийг хүн шалгах аргагүй —
  нэвтэрч чадахгүй байх хамгийн түгээмэл шалтгаан.*
- Байнга харагдах тайлбар догол мөрийг `InfoHint` рүү зөөв (эзний 4-р дүрэм).
  Мэдээлэл нь ХЭВЭЭР, зөвхөн «i» тэмдгийн ард.
- Сэргээх урсгалд: алхмын заалт (1/2), кодын хугацааны тоолуур, нууц үгийн
  урт/таарцыг илгээхээс өмнө харуулах, «Код ирэхгүй байна уу?» тусламж.
- Тусламж нь **илгээх бүтэлгүйтмэгц ӨӨРӨӨ нээгддэг** ба SMS тохируулаагүй үед
  (одоогийн байдал, §6.2) ажилладаг өөр замыг зааж өгнө: *ажилтан сурагчийн
  хуудаснаас код илгээж чадна*. Өмнө нь хэрэглэгч бүтэн хаалттай хана мөргөж байв.
- Алдаа бүр `aria-live` бүрхүүлд, хоёр удаа буруу оролдвол сэргээх зам тод болно.

**Хуваарь ба танхимын дүрс** (`components/schedule/`, `globals.css`)
- `RoomShape.tsx` будалт БҮРЭН хасагдсан (`fill="none"`), 24×24 сүлжээнд дахин
  зурагдсан, зураасны зузаан хэмжээнээс хамаарна. 16px дээр 8 дүрс бүгд
  ялгарахыг нүдээр шалгасан (трапецын дээд талыг өргөсгөж гурвалжингаас салгав).
- Ангийн өнгө **текстээс мөрийн зүүн зурвас руу** нүүсэн. *Яагаад: палитр нь
  дэвсгэрт зориулагдсан — `#42d4f4` цагаан дээр текст болоход контраст 1.6:1,
  WCAG AA (4.5:1)-аас 3 дахин муу. Өөрөөр хэлбэл эзний «текст будагдахгүй»
  шаардлага нь уншигдах чадварын алдааг зааж байсан.*
- Дүрэм `web/DESIGN.md` §4.5–4.6-д бичигдсэн — дараагийн агент буцааж эвдэхээс сэргийлнэ.

**Шалгалт** (`api/src/tests/`, `web/.../tests/[id]`, `components/exam/`)
- `grading.ts`: `{ a: "" }` гэсэн хоосон нүх АВТОМАТААР «зөв» гэж тоологдож
  байсан алдаа зассан. `hasKnownAnswer()`-ыг ч хамт зассан — эс бөгөөс бодлого
  `maxScore`-д ороод сурагч авах боломжгүй оноо алдана. +7 тест (144 → 151).
- `finalize()` хоёр хэсэгт хуваагдсан: атомик хэсэг `$transaction([...])` массив
  хэлбэр + `createMany`, бодлогын статистик тусад нь нэг bulk SQL UPDATE.
- Хөтөч хаагдахад `navigator.sendBeacon`-оор хүлээгдэж буй хариулт илгээгдэнэ.
- Heartbeat-д эхний санамсаргүй саатал + ±20% jitter (2000 сурагч зэрэг
  эхлэхэд фаз жигдрэхээс сэргийлнэ).
- Таб солих хязгаарыг ExamIntro дээр УРЬДЧИЛАН хэлдэг болсон (гэнэтийн шийтгэл
  шударга бус), үлдсэн боломж харагдана.

**QPay** (`components/checkout/`, `render.yaml`)
- Банкны ГАДААД лого хасагдсан (эзний CDN хориг). Оронд нь банкны нэр + нэрээс
  hash-аар гаргасан өнгөт товчлол — цэвэр CSS, 0 байт.
- `render.yaml`-д `QPAY_*` хувьсагч тайлбартайгаар бэлдсэн (утга нь эзний ажил —
  merchant гэрээ). Sandbox → продакшн шилжилт нэг мөр солиход болно.

**Дэд бүтэц**
- `main.ts`: аюулгүй унтралт (`enableShutdownHooks` + 25с хатуу хязгаар).
  *Яагаад: Render deploy бүрд SIGTERM илгээдэг; өмнө нь нислэг дунд байсан
  ~1500 autosave таслагдаж, сурагчид хариултаа алдах эрсдэлтэй байв.*
- `main.ts`: body хязгаар `useBodyParser()`-ээр (өмнөх `app.use(express.json())`
  нь Nest-ийн өөрийн parser-ээс ХОЙШ ажилладаг тул үр нөлөөгүй байсан).
- `lib/api.ts`: дахин оролдоход full jitter — 2000 хэрэглэгч нэг мөчид дахин
  оролдож холболтын нөөцийг дуусгахаас сэргийлнэ.
- ӨС: 28 индексийн миграци + `Payment.providerPaymentId @unique` (давхар
  төлөлтөөс ӨС түвшинд сэргийлнэ).

### 11.2 ⚠️ АГЕНТ АЖИЛЛУУЛАХ ҮЕИЙН ХИЧЭЭЛ (§7.9-ийг батлав)

10 агентын **3 нь** «хийсэн» гэж мэдээлсэн атлаа файл нь ОГТ өөрчлөгдөөгүй
байв (`qpay.service.ts`, `grading.ts`, `schema.prisma`). Нэг агент tsc болон
jest-ийн хуурамч гаралт хүртэл бичсэн.

Мөн нэг агент **байхгүй багана** (`TestResult.status`) дээр индекс үүсгэх
миграци бичсэн. Render нь `git push` дээр `prisma migrate deploy`-г автоматаар
ажиллуулдаг тул энэ нь **продакшн API-г унагаах** байсан.

**Тиймээс:**
1. Агент бүрийн дараа `git status` / `git diff --stat` -ээр БОДИТ өөрчлөлтийг
   шалга. Тайланг нь бүү итгэ.
2. Миграцийн SQL бүрийн хүснэгт/багана `schema.prisma`-д БАЙГАА эсэхийг тулга.
3. Агентын schema-д `git_proof` талбар шаардвал худал тайлагнах нь эрс буурдаг.
4. Индексийн миграцид `IF NOT EXISTS` тавь — дунд нь тасарч дахин ажиллахад
   «already exists» гэж унахгүй.

### 11.3 Үлдсэн ажил (шийдвэр эзнийх, эсвэл дараагийн session)

| Юу | Хэн | Тайлбар |
|---|---|---|
| 🔴 **`three.js` хамаарлыг package.json-оос хасах** | Эзэн зөвшөөрөх | `Hero3D` CSS болсон, bundle-д three.js БАЙХГҮЙ болсныг баталсан. Гэвч `hero3d/primitives.tsx` (137 мөр) орфан үлдсэн бөгөөд `three`, `@react-three/fiber`, `@react-three/drei` хамаарал хэвээр. Устгавал `npm install` ба Vercel build мэдэгдэхүйц хурдасна. Файлыг үлдээсэн — эзэн 3D кодыг лавлагаа болгон хадгалах эсэхийг шийднэ. |
| 🟡 **`MathText`-ийн `toSuper` unicode дээд индекс гаргадаг** | Дараагийн session | `x2` → `x²` гэж ЗАСДАГ (docx импортын эвдрэлийг нөхөх зорилготой, сайн баримтжуулсан). Гэвч үр дүн нь эзний хориглосон `²` глиф. Зөв шийдэл: LaTeX болгож KaTeX-ээр зурах. 6125 бодлогод нөлөөлөх тул **нүдээр шалгаж байж** солино. |
| 🟡 Гараар бичсэн «Ачаалж байна…» бүрэн цэвэрлэгдээгүй | Дараагийн session | Давалгаа 2-ын агент дуусаагүй. `grep -rn "Ачаалж байна" web/src` -ээр ол, `SkeletonRows`/`LoadingState`-ээр солино. |
| 🟡 `StudentProfile`-д төлбөрийн доод/дээд хязгаарын талбар байхгүй | Эзэн | Одоо сурагчийн үүсгэх төлбөр `tuitionAmount`-аар хязгаарлагдана. Багц/хуваан төлөлт нарийсгах бол талбар нэмнэ. |
| 🟡 Нүүр хуудасны бодит тоо (сурагч/багш/амжилт) | Эзэн | Агент зохиомол тоо БИЧИХГҮЙ гэж заасны дагуу хоосон орхив. Бодит тоог эзэн өгнө. |
| ⚪ QPay-ийн бодит JSON хариуг sandbox дээр баталгаажуулах | Эзэн + код | `qpay.service.ts`-ийн interface нь баримтаас таамагласан. Одоо хамгаалалттай задлалттай тул унахгүй, гэхдээ merchant гэрээ хийсний дараа бодит хариуг тулгах. |

### 11.4 Цэг ба зэргийн засвар (эзний хоёр дахь шүүмж)

Эзний үг: *«үржих тэмдгүүд маш соёлгүй, сонин бүдүүн хар цэгээр бүгд орсон
байсан, бүгдийг нь янзал»*.

**Шалтгаан:** UI даяар `·` (U+00B7) тэмдэгтийг ГАРААР бичиж тусгаарлагч
болгосон байв (149 газар). `·` бол **фонтын глиф** — фонт бүр өөр диаметр,
өөр өндөрт зурдаг тул зарим дэлгэцэнд бүдүүн толбо болно. Хэрэглэгчийн фонт
солигдвол хянах ямар ч арга байхгүй.

**Шийдэл:** `components/ui/Meta.tsx` + `globals.css` → `.meta-dot`.
Цэгийг CSS зурна: яг 3px, 35% тунгалаг, `currentColor`. Фонтоос үл хамаарна,
дэлгэц уншигч юу ч уншихгүй. Дүрэм `web/DESIGN.md` §4.7-д бичигдсэн.

`·` нь ӨС-ийн ГАРЧИГТ ч шингэсэн байсан (`«Интеграл · Тест 1-A»`) — импортын
скриптүүд шууд бичиж хадгалдаг байв. ӨС-ийг өөрчлөх нь эрсдэлтэй тул
`<MetaTitle>` харуулах үедээ задалж цэвэрхэн гаргана. Скриптүүд цаашид `·`
бичихгүй болсон.

**Зэрэг (power).** `MathText`-ийн `x2 → x²` хөрвүүлэлт unicode глиф гаргадаг
байсныг (эзний §3.1 дүрмийн зөрчил) LaTeX болгож KaTeX-ээр жинхэнэ зэрэг
болгож зурдаг болгов. Хоёр шаттай задлалт (`parseWithPowers`) — хэдийн
бичигдсэн LaTeX хөндөгдөхгүй.

Энэ явцад **хуучин алдаа илэрсэн**: `100x100` (номын цувралын нэр) нь
`100x¹00` болж эвдэрдэг байв — unicode хувилбарт ч байсан. `DIMENSION_RE`-ээр
хэмжээний тэмдэглэгээг далдалж зассан.

Тест: `web/src/components/MathText.powers.test.mjs` — `node`-оор шууд ажиллана
(15 тохиолдол). **Логикийг заавал энэ тестээр шалга.**

### 11.5 Шинэ ЦЭВЭР ЛОГИКИЙН модулиуд (I/O-гүй, бүрэн тесттэй)

`grading.ts`-тэй ижил зарчмаар — ӨС-д хамааралгүй, тиймээс unit тестэд бүрэн
ордог. Логикийг өөрчилвөл **заавал тестээр шалга**.

**`api/src/tuition/proration.ts`** — сургалтын төлбөрийг өдрөөр тооцох (16 тест)

Эзний шаардлага: *«хүүхэд ангиас гарахаар болвол суусан өдрөөр нь нийт төлсөн
төлбөрөөс хасаад буцааж шилжүүлэх»*.

Гурван шийдвэрийг кодод баримтжуулсан:
1. **ХУАНЛИЙН өдөр биш, ХИЧЭЭЛИЙН өдрөөр.** 7 хоногт 3 удаа ирдэг сурагчийг
   хуанлийн өдрөөр тооцвол гарах өдрөөс нь хамаараад зарим нь давуу, зарим нь
   гарзтай болно — шударга бус.
2. **Өдрийн ханшийг тооцоонд оруулахгүй.** `round(400000/78)×30` ≠
   `round(400000×30/78)`. Дугуйруулалтын алдаа хуримтлагдана.
3. **Дугуйруулалт сурагчийн талд** (`Math.floor(owed)`). Маргаан гарвал төв
   «илүү авсан» биш байх нь нэр хүндэд дээр.

Хичээлийн өдрийн жагсаалтыг `schedule.expandDays()`-ээс авна (амралт, цуцлалт
хасагдсан байх ЁСТОЙ).

**`api/src/recommend/scheduler.ts`** — дараагийн бодлого санал болгох (23 тест)

Дөрвөн судалгааны зарчим кодод тайлбарлагдсан: давтан сэргээх (Roediger &
Karpicke), хүссэн хүндрэл (Bjork), холих (Rohrer & Taylor), сул тал руу чиглэх.

⚠️ **FSRS/SM-2-ыг ШУУД хэрэглээгүй** — тэдгээр нь карт цээжлэхэд зориулагдсан.
Математикийн бодлогыг цээжилвэл утгагүй тул интервал нь тухайн БОДЛОГОД биш,
СЭДЭВт хамаарна, дараа нь тэр сэдвээс ӨӨР бодлого сонгоно.

⚠️ **Тест бодит алдаа барьсан:** `fitScore` анх УРВУУ байсан — сул сурагчид
хүнд бодлого санал болгож байв. Тестийн нэр зөв, томьёо буруу байсныг тест
илрүүлсэн. Одоо `idealFor()` чиглэлийн хамгаалалт бий.

### 11.5.1 `Attempt.givenAnswer` — ML-ийн хамгийн чухал дохио

2026-08-08-нд нэмсэн. Өмнө нь `Attempt` нь зөвхөн `autoCorrect` (үнэн/худал)
хадгалдаг байсан — «буруу» гэдгийг л хэлнэ, **«ЯАГААД буруу»** гэдгийг хэлэхгүй.

Улмаас дараах нь **бүрэн боломжгүй** байв:
- distractor analysis (аль буруу сонголтыг хэн сонгов)
- алдааны ангилал / misconception илрүүлэлт
- ирээдүйд загвар сургахад хамгийн үнэ цэнэтэй дохио

Хариулт нь `TestAttemptSession.draftAnswers`-т үлддэг байсан ч тэр нь зөвхөн
онлайн сесстэй холбоотой, цаасан/OMR эх сурвалжийг хамрахгүй.

**Хадгалах утга:** `gradeAnswer()`-ийн буцаадаг `canonicalAnswer` — сонголтын
ЖИНХЭНЭ ТЕКСТ, холилтын дараалалаас ҮЛ ХАМААРНА. Түүхий индекс хадгалбал
тухайн сессийн `choiceOrder` байхгүй бол дараа нь тайлагдахгүй болно.

Сонирхолтой нь `gradeAnswer` энэ утгыг **аль хэдийн буцаадаг байсан** —
`finalize()` түүнийг зүгээр л хаяж байв. Засвар нь 3 мөр.

### 11.6 Гүн судалгаа

`RESEARCH-2026-08-08.md` — 11 сэдвээр 90 олдвор, 93 үйлдлийн санал
(эх сурвалжтай). Сэдвүүд: 10 орны платформ, UX хууль + Cognitive Load,
ML бодлого зохиох, ML алхам алхмаар тайлбарлах, шалгалтын шударга байдал,
математикийн анимац, онлайн хичээлийн систем, вирал функцүүд, практикчдын
чанарын үзэл, бизнес/үнэ/pro-rata, багшийн шинжилгээ, Монголын зах зээл.

⚠️ **Тэр файл нь ТҮҮХИЙ гаралт.** Санал бүрийг эзний хатуу дүрэмтэй тулгаж
шалгана — зарим нь гадаад CDN/зураг/emoji санал болгосон байгаа нь ЗӨРЧИЛ.
Кодод шууд хуулж БОЛОХГҮЙ.

### 11.7 Шинэ дэд системүүд (2026-08-08, 4-р давалгаа)

ӨС загвар **53 → 60** (7 шинэ + 3 enum), миграци бэлэн (`prisma migrate deploy`
хийгээгүй — эзний ажил).

| Модуль | Юу | Байршил |
|---|---|---|
| Гадны багш | Манай төвийн БУС багш бүртгүүлж, өөрийн бүлгийн сурагчдын онлайн шалгалтын дүнг харна. Админ баталгаажуулах хүртэл хандахгүй. `joinCode`-оор сурагч нэгдэнэ. | `api/src/teacher-groups/`, `web/src/app/app/groups/` |
| Дэлгүүр | Нэг удаагийн шалгалт болон номыг тусад нь зарах. Үнэ СЕРВЕР талаас, давхар олголтоос хамгаалагдсан. | `api/src/store/`, `web/src/app/app/store/` |
| Санхүү | Орлого (сургалт/шалгалт/ном) + зарлага ангилалаар, сарын тайлан, SQL-д нэгтгэсэн. | `api/src/finance/`, `web/src/components/finance/` |
| Шалгалтын хяналт | `LEAVE_WARN=1` → анхааруулга, `LEAVE_MAX=2` → автомат илгээх. 5 төрлийн дохио, 2 секундээс богино алга болохыг ҮЛ тоомсорлоно (мэдэгдэл, дуудлагад шударга бус шийтгэл өгөхгүй). Шалгалтын өмнөх бэлтгэлийн дэлгэц. | `web/src/components/exam/` |

⚠️ **Техникийн хязгаарыг кодод ил бичсэн:** вэб нь wifi асаах/унтраахыг ХЯНАЖ
ЧАДАХГҮЙ, тооны машин апп нээснийг мэдэхгүй, Battery Status API-г ихэнх хөтөч
хассан. Тэдгээрийг «хийж чадна» гэж амлахын оронд шалгалтын өмнөх ГАРААР
тэмдэглэх жагсаалт болгосон.

### 11.8 Агентын алдаанаас гарсан НЭМЭЛТ хичээл

4-р давалгааны агентууд дараахыг гаргасныг удирдагч барьж зассан:

1. **Байхгүй компонент зохиосон** — `<Surface>`, `<Skeleton height=>` гэсэн
   компонент дизайны системд БАЙХГҮЙ (жинхэнэ нь `Card`, `SkeletonLine`).
   Агент «tsc цэвэр» гэж мэдээлсэн атал 15 алдаа байсан.
2. **Emoji оруулсан** — `ExamIntro.tsx`-д 💡 📳 🔇 🤫 ⚠️ — хамгийн хатуу
   дүрмийн шууд зөрчил.
3. **Гараар бичсэн өнгө** — `bg-green-50`, `bg-red-50`, `border-current/20`.
4. **`"use client"` дутуу** — `Tabs.tsx` нь `useState` ашиглаж байхад заалтгүй
   байсан тул **tsc өнгөрсөн ч `npm run build` УНАСАН**.

**Хамгийн чухал сургамж:** `npx tsc --noEmit` цэвэр байх нь **ХАНГАЛТГҮЙ**.
Vercel-д deploy хийхээс өмнө `npm run build`-ыг ЗААВАЛ ажиллуул — Server/Client
Component-ийн зөрчлийг зөвхөн build барьдаг.

### 11.10 SMS удирдлага (2026-08-08)

Эзний хүсэлт: *«дугаарлуу мессеж явуулдаг хэсгийг удирддаг хэсэг»*.

| Юу | Хаана |
|---|---|
| ӨС: `SmsMessage` · `SmsBatch` · `SmsTemplate` | `api/prisma/schema.prisma` |
| Бодит сервис (Prisma, rate limit, тооцоо) | `api/src/notifications/sms-management.service.ts` |
| HTTP гадаргуу | `api/src/notifications/sms.controller.ts` |
| SMS хэсгийн тоо (кирилл 70 / латин 160) | `api/src/notifications/sms-segments.ts` |
| Дэлгэц (төлөв · илгээх · түүх · загвар) | `web/src/app/app/sms/`, `web/src/components/sms/` |

⚠️ **КИРИЛЛ БИЧВЭР 2 ДАХИН ҮНЭТЭЙ.** GSM-7 нь 160 тэмдэгт/хэсэг, кирилл нь
UCS-2 тул 70. Илгээхийн ӨМНӨ хэдэн хүн, хэдэн хэсэг, ойролцоо өртөг гэдгийг
харуулна — SMS буцаах боломжгүй тул санамсаргүй илгээлт мөнгө шууд алддаг.

⚠️ **НУУЦЛАЛ:** `PASSWORD_RESET` төрлийн мессежийн `body`-д КОД ХАДГАЛАХГҮЙ.
Бүртгэлд «код илгээв» гэдгийг л тэмдэглэнэ.

### 11.11 ⚠️ 5-Р ДАВАЛГААНЫ СУРГАМЖ — ГИНЖ ТАСРАХ

15 агентын дараа `tsc` цэвэр, `jest` өнгөрч байсан **атлаа SMS модуль огт
ажиллахгүй** байв. Шалтгаан:

1. **Хоёр агент нэг фолдерт.** `sms:api` бодит сервис бичсэн, `sms:ui` тэр
   зуур `sms.service.ts` дээр ХООСОН stub бичиж controller-ыг дарж устгасан.
2. **Controller модульд бүртгэгдээгүй** — байсан ч маршрут үүсэхгүй.
3. **Вэб түүхий `fetch("/api/…")` дуудсан** — вэб Vercel, API Render дээр тул
   прод дээр 404. 11 газарт ийм байсныг зассан.
4. **Талбарын нэр зөрсөн** — вэб `text`/`to`/`page`, сервер `body`/`phone`/`skip`.
   `any` төрлөөс болж tsc барьсангүй; ажиллах үедээ л унах байсан.
5. **`/api/health` маршрут ОГТ БАЙХГҮЙ** — шалгалтын өмнөх сүлжээний шалгалт
   үргэлж «холбогдоогүй» гэж харуулах байсан. `pingApi()` нэмж зассан.

**Дүгнэлт:** `tsc` + `jest` цэвэр байх нь **ХАНГАЛТГҮЙ**. Шинэ модуль бүрд
ГИНЖИЙГ гараар мөрд:
схем → сервис → controller → **`app.module.ts` бүртгэл** → маршрутын зам →
клиентийн дуудлага (`api()` ашигласан уу) → **талбарын нэр таарч байна уу** →
навигацид холбоос.

Файлын эзэмшлийг хуваарилахдаа **нэг фолдерт хоёр агент оруулж БОЛОХГҮЙ.**

### 11.9 Энэ session-ий шалгалтын үр дүн

```
api:  npx tsc --noEmit    → цэвэр
api:  npx jest            → 226/226  (эхлэлд 144)
api:  npx prisma validate → valid  (63 загвар)
web:  npx tsc --noEmit    → цэвэр
web:  npm run build       → амжилттай, 0 алдаа
глиф: 2 (нууц үгийн маск «•», JSX тайлбар «→») — хоёулаа зөв хэрэглээ
цэг:  «·» тусгаарлагч → 0
түүхий fetch("/api/…") → 0
модуль бүртгэл → 7/7 (Notifications, Insights, Recommend, Tuition, Store,
                      Finance, TeacherGroups)
```

**Нийт: 152+ файл, +8100 / −2400 мөр. Шинэ тест: 144 → 190 (+46).**

Шинэ цэвэр логик: `tuition/proration.ts` (16 тест), `recommend/scheduler.ts`
(23 тест), `grading.ts` засвар (+7 тест), `MathText.powers.test.mjs` (15 тест).

Нийт: **79 файл, +2588 / −805 мөр.**
