# Pi.mn — төслийн ЦОРЫН ГАНЦ баримт

> **Шинэ session эндээс эхэл.** Энэ файл нь төслийн бүх мэдлэгийг агуулна:
> одоогийн байдал, эзний шийдвэр, цаг иддэг урхи, нээлттэй эрсдэл.
> Уншсаны дараа кодыг дахин судлах шаардлагагүй.
>
> Сүүлд шинэчилсэн: **2026-08-08**

---

## 0. Энэ баримтыг хэрхэн ашиглах

**Энэ файл бол ЦОРЫН ГАНЦ эх сурвалж.** Өмнө нь 10 гаруй баримт зэрэгцэн
оршиж, аль нь хүчинтэйг хэн ч мэдэхгүй байв. Тэдгээрийг `docs/archive/`-д
зөөсөн — **лавлагаа болгон уншиж болно, гэхдээ энэ файл давамгайлна.**

### Шинэ мэдээлэл нэмэх дүрэм

Шинэ зүйл мэдвэл **сэдвийнх нь бүлэгт нэм** — төгсгөлд огноотой блок
нэмэхгүй. Тэр байдлаар л энэ файл өмнө нь эвдэрсэн: «deploy» гэсэн мэдээлэл
хоёр бүлэгт, «урхи» дөрвөн бүлэгт тарсан байв.

| Юу мэдвэл | Хаана бичих |
|---|---|
| Эзэн шийдвэр гаргавал | §3 Дүрэм эсвэл §9 Эрсдэл |
| Цаг идсэн алдаа олвол | §8 Урхи (шалтгаантай нь) |
| Шинэ дэд систем нэмвэл | §7 |
| Дизайны шийдвэр | §6 |
| Дуусаагүй ажил | §9 |

**Гүйцэтгэсэн ажлын тайлан ЭНД БИЧИХГҮЙ** — тэр нь git түүхэнд байна.
Зөвхөн *хожим дахин олж мэдэхэд үнэтэй* зүйлийг бич.

### Бусад файл

| Файл | Юу | Устгаж болох уу |
|---|---|---|
| `CLAUDE.md` | Claude Code АВТОМАТААР уншдаг заагч | ❌ механик шаардлагатай |
| `web/CLAUDE.md` → `web/AGENTS.md` | Next.js хувилбарын анхааруулга | ❌ механик шаардлагатай |
| `docs/research/` | 2026-08-08 судалгаа (2720 мөр, түүхий) | лавлагаа |
| `docs/archive/` | Хуучин баримтууд | лавлагаа |
| `api/src/gateways/README.md` | QPay-ийн техник дэлгэрэнгүй | кодын хажууд байх нь зөв |
| `web/src/components/checkout/README.md` | Checkout UI-ийн гэрээ | кодын хажууд байх нь зөв |

---

## 1. Юу вэ

**Шинэ Ирээдүйн Эзэд** сургалтын төвийн дотоод платформ.
ЭЕШ-ийн математик + нийгмийн ухааны бэлтгэл, танхим + онлайн.

- **Прод вэб:** https://web-one-pi-59.vercel.app
- **Прод API:** https://pimn-api.onrender.com/api
- **GitHub:** https://github.com/mrbuus/Pi (branch `main`)

### Хэмжээ (2026-08-08)

| | | Эх сурвалж |
|---|---|---|
| Прод сурагч | ~359 | `BURTGEL-2027-1.xlsx` импорт |
| Ажилтан | 16 | цалингийн хүснэгт |
| Анги | 21 | прод ӨС |
| Багш | 13 | прод ӨС |
| Хуваарийн мөр | 77 | `schedule-plan.cjs` |
| Бодлого | ~6125 | прод ӨС |
| Prisma загвар | 63 | `schema.prisma` |
| Миграци | 31 | `prisma/migrations/` |
| API unit тест | 226 (15 файл) | бүгд өнгөрдөг |
| Вэб хуудас / компонент | 40+ / 200+ | |

---

## 2. Ажиллуулах ба шалгах

```bash
# API (терминал 1)
cd /Users/mr.buus/Intern/Pi.mn/api && npm run start:dev     # :3000

# Вэб (терминал 2)
cd /Users/mr.buus/Intern/Pi.mn/web && npm run dev           # :3001
```

### Шалгалт — ГУРВУУЛАН заавал

```bash
cd api && npx tsc --noEmit && npx jest        # 226/226
cd web && npx tsc --noEmit && npm run build   # build ЗААВАЛ, tsc хангалтгүй
```

**⚠️ `tsc` цэвэр байх нь ХАНГАЛТГҮЙ.** Гурван төрлийн алдаа зөвхөн build
эсвэл ажиллуулах үед л илэрдэг — дэлгэрэнгүйг §8.1-ээс үз.

### Deploy-ийн өмнөх ачаалалтын шалгалт

```bash
cd api && npm run build
node -e "setTimeout(()=>process.exit(0),15000); require('./dist/src/main')" 2>&1 \
  | grep -cE "Nest can't resolve"     # → 0 (модулийн хамаарал)

node -e "setTimeout(()=>process.exit(0),15000); require('./dist/src/main')" 2>&1 \
  | grep -c "Mapped {/api/api"        # → 0 (давхар префикс)
```

### Туршилтын бүртгэл (нууц үг = утасны дугаар)

| Эрх | Утас |
|---|---|
| ADMIN | `99294266` (Б. Өсөхбаяр) |
| TEACHER_PLUS | `80333020` (У. Эмүжин) |
| STUDENT | `80601209` (Гантөмөр Анар) |

> ⚠️ **Локал** ӨС-д админы нууц үг утастай таарахгүй байсан. Продакшнд зөв.
> Локалд шалгах бол эхлээд bcrypt-ээр дахин тавина.

---

## 3. Эзний ХАТУУ дүрмүүд

Эдгээрийг зөрчвөл ажил хүчингүй. Бүгд бодит залруулгаас гарсан.

1. **Математик бичвэр ҮРГЭЛЖ LaTeX** (`MathText`). `π`, `²`, `⅓`, `·`, `√`
   гэх мэт энгийн тэмдэгт **хэзээ ч** болохгүй.
   *Эзний үг: «маш хүндэтгэлгүй, муухай, заваан харагдаж байна».*
2. **Emoji болон unicode глиф ХОРИОТОЙ** — зөвхөн `lucide-react`.
   *Эзний үг: «эмодзи огт оруулахгүй, зориулалтын вэб сайтаас icon авах ёстой».*
3. **Тусгаарлагч цэг `·` гараар бичихгүй** — `components/ui/Meta.tsx`.
   Шалтгаан §6.3-т.
4. **Нууц үг = утасны дугаар**, автоматаар. «Чанартай нууц үг» санал
   болгохыг эзэн шууд татгалзсан.
5. **Жижиг тайлбар бичвэр олшруулахгүй** — «цэвэрхэн ухаалаг дизайныг хямд
   харагдуулдаг». Тайлбар хэрэгтэй бол `InfoHint` («i» тэмдэг).
6. **«Математик болон Нийгмийн ухаанд төвлөрсөн»** гэсэн үг хориотой.
   Оронд нь: «элсэлтийн шалгалтад бэлдэх + математикийн хоцрогдол арилгах
   танхимын сургалт».
7. **«Мэдээлэл авах» маягт** нүүр хуудсанд байхгүй (эзэн хассан).
8. **Интернэт удаан байхад UI муудах ёсгүй.** Видео / WebGL / гадаад фонт /
   CDN / гадаад зураг ХОРИОТОЙ. Эффект бүр цэвэр CSS.
9. **Функц устгахгүй.** Дизайн сайжруулна гэдэг нь юм хасна гэсэн үг биш.
10. **Нэг зүйл хийгээд шууд push хийхгүй** — өдрийн төгсгөлд эсвэл эзэн хэлэхэд.
11. **Гарагийн дугаарлалт**: эзэн МОНГОЛ (1=Даваа…7=Ням), ӨС нь JS
    (0=Ням…6=Бямба). Андуурвал **бүх хуваарь нэг өдрөөр гулсана**.
12. **Гар утас тэргүүнд** (375px). Монголд хэрэглээ гар утсан дээр давамгайлдаг.

---

## 4. Deploy — ЭНЭ НЬ ТЭГШ БУС

| | Автомат уу |
|---|---|
| **Render (API)** | ✅ `git push` дээр өөрөө deploy + `prisma migrate deploy` |
| **Vercel (вэб)** | ❌ **ХИЙДЭГГҮЙ.** Гараар ажиллуулна |

```bash
cd /Users/mr.buus/Intern/Pi.mn/web && \
  npx vercel@latest --prod --yes --scope usukhbayar0525-6364s-projects
```

> ⚠️ `--scope` заавал — `.vercel/project.json` дээрх `orgId` хуучирсан тул
> үүнгүйгээр «Not authorized» гэж унана.

Push хийгээд Vercel-ийг мартвал frontend хуучин хэвээр үлдэнэ.
**2026-07-30-нд яг ингэж 12 цаг хуучин хувилбар прод дээр байсан.**

### Deploy амжилттай эсэхийг ШАЛГАХ

Render «deploy хийлээ» гэсэн ч **шинэ код гарсан гэсэн үг биш**. Апп боохдоо
унавал Render хуучин instance-аа үлдээдэг бөгөөд API нь HTTP 200 буцаасаар
байна — гаднаас нь бүх зүйл зөв харагдана.

```bash
# Шинэ маршрут руу токенгүй хандах:
curl -s -o /dev/null -w "%{http_code}" https://pimn-api.onrender.com/api/sms/status
# 401 = маршрут БАЙНА (шинэ код) · 404 = хуучин код хэвээр
```

### Прод ӨС дахин үүсгэх дараалал

Бүх скрипт идемпотент:

```bash
npx prisma migrate deploy                             # 0) схем
node prisma/upsert-staff.cjs --commit --update-names  # 1) ажилтан
node prisma/import-students.cjs                       # 2) сурагч
node prisma/seed-prod-via-api.cjs                     # 3) хуваарь
```

### Миграцийн аюулгүй байдал

Шинэ миграци бичихдээ:
- `CREATE TABLE/INDEX` бүрд **`IF NOT EXISTS`** — deploy дунд тасарч дахин
  ажиллахад унахгүй
- `ADD CONSTRAINT` нь `IF NOT EXISTS`-ыг дэмждэггүй → `DO $$ ... pg_constraint`
  шалгалт бич
- Миграцийн SQL-ийн хүснэгт/багана бүрийг `schema.prisma`-тай **тулгаж шалга**.
  2026-08-08-нд агент байхгүй багана (`TestResult.status`) дээр индекс бичсэнийг
  барьсан — тэр нь прод API-г унагаах байсан.
- **Хуучин миграцийн файлыг ХЭЗЭЭ Ч бүү өөрчил** — checksum зөрж
  `migrate deploy` унана.

> ⚠️ Прод вэб дээр шалгахдаа **HTML-ээс grep хийж болохгүй** — client
> компонентийн текст эхний HTML-д байхгүй. Браузераар үзэх эсвэл DOM шалгана.

---

## 5. Архитектур

```
Pi.mn/
├── api/                 NestJS + Prisma + PostgreSQL
│   ├── src/             38 модуль
│   └── prisma/
│       ├── schema.prisma            63 загвар
│       ├── migrations/              31
│       ├── schedule-plan.cjs        ← хуваарийн ЦОРЫН ГАНЦ эх сурвалж
│       ├── seed-year-schedule.cjs   ← локал ӨС рүү шууд
│       └── seed-prod-via-api.cjs    ← прод руу API-аар
└── web/                 Next.js 15 App Router + Tailwind v4
    └── src/
        ├── app/         40+ хуудас
        ├── components/  200+ компонент
        └── lib/api.ts   api() · uploadFile() · fileUrl() · pingApi()
```

### Эрхийн загвар

`ADMIN` · `TEACHER_PLUS` · `TEACHER` · `STUDENT` · `PARENT` · `BUYER`

- Контентын эзэмшил **TEACHER_PLUS**-д (2026-07-27 шийдвэр)
- **Мөнгө, SMS, эрх солих нь зөвхөн ADMIN** — SMS нь өртөгтэй бөгөөд
  буцаах боломжгүй
- Энгийн `TEACHER` — өөрийн ангийн ирц/даалгавар, хуваарийг зөвхөн ХАРНА

### Клиент талын сүлжээ — `lib/api.ts`

**Түүхий `fetch("/api/…")` ХЭЗЭЭ Ч бичихгүй.** Вэб нь Vercel, API нь Render
дээр байдаг тул харьцангуй зам прод дээр 404 болно. Мөн эрхийн толгой
дамжихгүй тул 401 авна.

| Функц | Хэзээ |
|---|---|
| `api<T>(path, opts)` | бүх энгийн дуудлага. Суурь хаяг, токен, timeout, jitter-тэй retry, монгол алдааны мессежийг өөрөө зохицуулна |
| `uploadFile(file)` | файл байршуулах (FormData, 30с timeout) |
| `fileUrl(key)` | `<img src>`-д — токеныг query-д хавсаргана |
| `pingApi()` | сервер хүрэлцэхүйц эсэх (`/health` маршрут БАЙХГҮЙ) |

---

## 6. Дизайны гэрээ

Хэд хэдэн агент зэрэг ажилладаг тул хүн бүр өөрийн зөнгөөр шийдвэл 10 өөр
дизайн гарна. **UI бичихээс өмнө энэ бүлгийг унш.**

### 6.1 Таслахгүй дүрэм

1. **Сүлжээнээс нэмэлт байт татахгүй** — гадаад фонт/зураг/видео/WebGL/CDN/
   шинэ npm сан ХОРИОТОЙ. Эффект бүр цэвэр CSS.
2. **Функц устгахгүй.**
3. **Өнгийг гараар бичихгүй** — `#fff`, `text-blue-500` БОЛОХГҮЙ. Зөвхөн
   токен: `bg-surface` `bg-panel` `bg-bg` `text-ink` `text-ink-dim`
   `border-line` `text-brand` `text-brand-soft` `bg-brand-bright`
   `text-on-brand` `success/warning/error/info`.
   Өнгөт дэвсгэр дээрх текст `text-on-brand` (`text-white` БИШ).
4. **Зөвхөн өөрт оногдсон файлаа зас.** Өөр агентын файл руу орвол дарж бичнэ.

### 6.2 Бэлэн компонент — шинээр бүү зохио

| Файл | Экспорт |
|---|---|
| `ui/StateBlock.tsx` | `LoadingState` · `ErrorState(onRetry)` · `EmptyState` |
| `ui/Skeleton.tsx` | `SkeletonLine(width,height)` · `SkeletonRows(rows)` · `SkeletonCard` |
| `ui/Surface.tsx` | `Card` · `SectionHeader` · `PageHeader` |
| `ui/Meta.tsx` | `Dot` · `Meta(items)` · `MetaTitle` |
| `ui/InfoHint.tsx` | **default export** (нэрлэсэн импорт БИШ) |
| `ui/ConnectionStatus.tsx` | сүлжээ тасрахад доод тууз |

> ⚠️ `Surface` гэсэн **компонент байхгүй** — файлын нэр л тийм. Агент энэ
> нэрийг зохиогоод build унагаасан удаатай.

### 6.3 Мета тусгаарлагч — `·` тэмдэгт ХОРИОТОЙ

`·` (U+00B7) болон `•` нь **фонтын глиф**. Фонт бүр өөр диаметр, өөр өндөрт
зурдаг тул зарим дэлгэцэнд бүдүүн толбо болно —
*эзний шүүмж: «маш соёлгүй, сонин бүдүүн хар цэг».*

```tsx
<Meta items={[room, teacher, time]} />   // олон зүйл, null-ыг алгасна
<Dot />                                  // ганц тусгаарлагч
<MetaTitle>{test.title}</MetaTitle>      // ӨС-ээс ирсэн «А · Б» гарчиг
```

Цэгийг CSS зурна (`.meta-dot`): яг 3px, 35% тунгалаг, `currentColor`.
Мөр (string) заавал байх газарт (`title=`, `aria-label=`) зураас « — ».

**Үл хамаарах:** «100×100», «200×200», «300×300» — номын цувралын бодит нэр.

### 6.4 Өгөгдлөөс ирдэг өнгө (анги, банк, шошго)

**Өгөгдлөөс ирдэг өнгийг ТЕКСТ дээр ХЭЗЭЭ Ч тавихгүй.**

`lib/classroomColor.ts`-ийн 13 өнгө нь **дэвсгэрт** зориулж сонгогдсон.
Текст болгоход зарим нь уншигдахгүй: `#42d4f4` цагаан дээр 1.6:1,
`#3cb44b` 2.6:1, `#f58231` 2.9:1 — бүгд WCAG AA (4.5:1)-аас доогуур.
`text-ink` нь 16.4:1.

| Зөв | Буруу |
|---|---|
| зүүн зурвас (`border-left: 3px`) | `style={{ color }}` текст дээр |
| дугуй цэг / жижиг тэмдэг | дүрсийн `fill` эсвэл `stroke` |
| маш зөөлөн дэвсгэр (12–18%) | ханасан дэвсгэр дээр текст |

Бэлэн анги: `.class-row` + `.class-row-interactive`.
Өнгийг `style={{ "--cls": color } as React.CSSProperties}`.

### 6.5 Танхимын дүрс (`schedule/RoomShape.tsx`)

**БУДАЛТГҮЙ** (`fill="none"`), зөвхөн зураас.
*Эзний шаардлага: «дүрсийг нь будалгүй, хүн шууд хараад мэдэхээр ТОД».*

Жижиг хэмжээнд дүрсийн танигдах чадвар **силуэтийн ирмэгээс** хамаардаг —
дотоод будалт ирмэгийн контрастыг бууруулж, квадрат/ромбо, трапец/гурвалжин
хосыг ялгахад хүндрүүлдэг.

Зураас нь `currentColor` (ink) — ангийн өнгийг дүрс рүү дамжуулахгүй.
Геометр 24×24 сүлжээнд, оптикоор ижил жинтэй. Зузаан
`(24 / size) × strokePxFor(size)`.
**Дүрсийг өөрчилвөл 16px дээр 8 дүрс бүгд ялгарч байгааг нүдээр шалга.**

### 6.6 Хэмнэл, гүн, нягтрал

- Хугацаа: `--dur-micro` 120ms (hover) · `--dur-base` 180ms · `--dur-enter` 260ms
- Зөвхөн `transform` ба `opacity` дүрсжүүлнэ — `width`/`top`/`margin`
  дүрсжүүлбэл layout дахин тооцоологдож урт жагсаалт гацна
- Гүн: `.elev-1` карт · `.elev-2` hover/dropdown · `.elev-3` модал
- Интерактив картад `.surface-interactive`, жагсаалтын мөрд `.row-interactive`
- Мөрийн өндөр: `--row-compact` 28px · `--row-base` 32px · `--row-roomy` 38px
- Цайвар горим ӨГӨГДМӨЛ (жижиг математик тэмдэгт уншихад дээр), харанхуй сонголт
- `prefers-reduced-motion` үед бүх анимац унтарна

### 6.7 Хоосон / ачаалж буй / алдааны төлөв

**Хоосон цагаан дэлгэц хэзээ ч харуулахгүй.**

- Ачаалж байна → `.skeleton` ангиар **бодит бүтцийн хэлбэр**. «Ачаалж байна…»
  гэсэн ганц мөр хангалтгүй.
- Хоосон → яагаад хоосон, юу хийвэл дүүрэхийг хэлсэн товч тайлбар + товч
- Алдаа → монголоор ойлгомжтой мессеж + «Дахин оролдох». Техникийн англи
  алдаа хэрэглэгчид харуулахгүй. `console.error`-оор залгих нь «мөнхөд
  ачаалж байна» гэсэн үр дүн өгнө.

### 6.8 Монгол бичвэрийн онцлог

Монгол үг латинаас **20–30% урт**. 375px дээр багтахгүй болно.
Гарчигт `.cyrillic-heading`, шошго/товчинд `.cyrillic-tight`.
Кирилл үсгийг `rotate()` эсвэл `writing-mode: vertical-rl`-ээр эргүүлбэл
гажина — эргүүлэх шаардлагатай бол SVG `<tspan>` эсвэл дүрс ашигла.

### 6.9 Хүртээмж

- Интерактив бүх зүйл гарнаас хүрнэ; `outline-none` гараар бичиж БҮҮ ус
- Дүрс дангаар байвал `aria-label`
- **Өнгө дангаараа утга илэрхийлэхгүй** — дүрс эсвэл текст дагалдана
  (өнгө ялгадаггүй хэрэглэгч ~8% эрэгтэй)
- Модалд `role="dialog"` + `aria-modal` + `aria-labelledby` + Escape + фокус занга
- Хүрэх талбай 44×44px-ээс багагүй

### 6.10 Бичвэрийн өнгө аяс

Монгол, товч, хүндэтгэлтэй. Техникийн үг («API», «server», «null»)
хэрэглэхгүй. Тушаах биш, тайлбарлах. Айлган сүрдүүлэх, ичээх, буруутгах
өнгө аяс ХОРИОТОЙ.

---

## 7. Дэд системүүд

### 7.1 Хуваарь

**Хоёр харагдац:**

1. **Ерөнхий хуваарь** (`PatternOverview.tsx`) — босоо 2 бүс (Баруун 4 /
   Зүүн 4) × 4 давтамжийн багана (Да·Лх·Ба / Мя·Пү / Бямба / Ням).
   Танхим тогтмол эрэмбэтэй: 501→502→503→504→403→404→405→301→302.
   *Яагаад 7 өдрийн багана биш: хуваарь 1,3,5-д яг ижил давтагддаг тул
   гурав давтахын оронд нэг удаа харуулбал бүтэн жил нэг дэлгэцэд багтана.*
2. **Долоо хоногийн төлөвлөгөө** (`WeekBuilder.tsx`) — огноот, сэдэв зоох.

**Гурван хамрах хүрээтэй засвар** (`EntryActionPanel.tsx`):
- Зөвхөн энэ өдөр → зөөх / цуцлах / сэдэв
- **Энэ ба цаашдын бүх** → `POST /schedule/:id/split`
  *Яагаад PATCH биш: PATCH нь өнгөрсөн рүү ч үйлчилж, 3 сарын өмнөх ирцийн
  бүртгэл «өөр цагт болсон» мэт харагдана.*
- Бүх давтамж → устгах (улаан, 2 алхамт)

Танхимын дүрс: 501=бөөрөнхий · 502=гурвалжин · 503=трапец · 504=квадрат ·
403=ромбо · 404=таван өнцөгт · 405=тэгш өнцөгт · Онлайн=дэлгэц.
**Өнгө = анги, дүрс = танхим.**

Бүх хичээл 120 минут. Хуваарийн эх сурвалж: `api/prisma/schedule-plan.cjs`.

### 7.2 Нэвтрэлт ба нууц үг

- Нэвтрэх: утас / имэйл / нэвтрэх нэр — гурвуулаа нэг талбараар
- **«Нууц үг харах» товч бүх талбарт.** Анхны нууц үг = 8 оронтой утасны
  дугаар; од болгон нуусан 8 цифрийг хүн шалгах аргагүй — нэвтэрч чадахгүй
  байх хамгийн түгээмэл шалтгаан. Өгөгдмөл ХААЛТТАЙ (мөрдөгчөөс хамгаална).
- Сэргээх урсгал 2 алхам (3 биш): код шалгах тусдаа endpoint нь брутфорсын
  нэмэлт гадаргуу үүсгэнэ
- **«Код ирэхгүй байна уу?» тусламж** — OTP урсгалын хамгийн том нүх.
  Илгээх бүтэлгүйтмэгц ӨӨРӨӨ нээгдэж, ажилладаг өөр замыг (ажилтнаас код
  авах) зааж өгнө
- Нууц үг солиход `passwordChangedAt` бүх сессийг таслана

### 7.3 Шалгалтын хөдөлгүүр

- Сервер session: дараалал анх нэг удаа үүсээд **хөлдөнө**, таймерыг сервер барина
- `buildChoiceOrder` — сонголтыг холино. Импортын placeholder (`["A".."E"]`)
  бол байрлал СОЛИХГҮЙ (үсэг = текст доторх хувилбарын цорын ганц холбоос)
- Autosave 1.2с debounce. **Хөтөч хаагдахад `navigator.sendBeacon`** —
  debounce хүлээлтэд байсан хариулт алдагдахгүй
- Heartbeat 20с ± 20% jitter + эхний санамсаргүй саатал — 2000 сурагч
  зэрэг эхлэхэд фаз жигдэрч цохилт үүсэхээс сэргийлнэ
- **Шалгалтаас гарах**: 1 дэх удаа анхааруулга, 2 дахьд автоматаар илгээх
  (`LEAVE_WARN=1`, `LEAVE_MAX=2`). 2 секундээс богино алга болохыг ҮЛ
  тоомсорлоно — мэдэгдэл, дуудлагад шударга бус шийтгэл өгөхгүй.
  **ExamIntro дээр УРЬДЧИЛАН хэлнэ** — гэнэтийн шийтгэл шударга бус
- `finalize()` хоёр хэсэгт: атомик хэсэг `$transaction([...])` + `createMany`,
  бодлогын статистик тусад нь bulk SQL (transaction 1-2с барихаас сэргийлнэ)

**Техникийн бодит хязгаар — амлаж БОЛОХГҮЙ:**
вэб нь wifi асаах/унтраахыг ХЯНАЖ ЧАДАХГҮЙ · тооны машин апп нээснийг
мэдэхгүй (зөвхөн таб солигдсоныг) · Battery Status API-г ихэнх хөтөч хассан ·
мэдэгдэл унтраасан эсэхийг мэдэх арга байхгүй. Эдгээрийг шалгалтын өмнөх
**гараар тэмдэглэх жагсаалт** болгосон.

### 7.4 Дүгнэлт — `tests/grading.ts` (цэвэр логик, тесттэй)

- `TEXT` горим: `isCorrect` flag-аар (үсэг/байрлалаас ангид)
- `LETTER` горим: placeholder choices үед үсгээр тулгана
- **Хоосон нүх ХЭЗЭЭ Ч зөв болохгүй.** `{ a: "" }` гэсэн объект дээд талын
  хамгаалалтыг давж, `tokensMatch("","")` → үнэн буцааж байсан алдааг
  2026-08-08-нд зассан. `hasKnownAnswer()`-ыг ч хамт зассан — эс бөгөөс
  бодлого `maxScore`-д ороод сурагч авах боломжгүй оноо алдана
- Хариу түлхүүргүй бодлого `maxScore`-оос хасагдана

### 7.5 Цэвэр логикийн модулиуд (I/O-гүй, бүрэн тесттэй)

`grading.ts`-тэй ижил зарчим — ӨС-д хамааралгүй тул unit тестэд бүрэн ордог.
**Логикийг өөрчилвөл заавал тестээр шалга.**

**`tuition/proration.ts`** — төлбөрийг өдрөөр тооцох (16 тест)

Эзний шаардлага: *«хүүхэд ангиас гарахаар болвол суусан өдрөөр нь нийт
төлсөн төлбөрөөс хасаад буцааж шилжүүлэх»*.

1. **ХУАНЛИЙН өдөр биш, ХИЧЭЭЛИЙН өдрөөр.** 7 хоногт 3 удаа ирдэг сурагчийг
   хуанлийн өдрөөр тооцвол гарах өдрөөс нь хамаараад зарим нь давуу, зарим
   нь гарзтай болно — шударга бус.
2. **Өдрийн ханшийг тооцоонд оруулахгүй.** `round(400000/78)×30` ≠
   `round(400000×30/78)` — дугуйруулалтын алдаа хуримтлагдана.
3. **Дугуйруулалт сурагчийн талд** (`Math.floor`). Маргаан гарвал төв
   «илүү авсан» биш байх нь нэр хүндэд дээр.

Хичээлийн өдрийн жагсаалтыг `schedule.expandDays()`-ээс авна.

**`recommend/scheduler.ts`** — дараагийн бодлого санал болгох (23 тест)

Дөрвөн зарчим: давтан сэргээх (Roediger & Karpicke) · хүссэн хүндрэл (Bjork) ·
холих (Rohrer & Taylor) · сул тал руу чиглэх.

⚠️ **FSRS/SM-2-ыг шууд хэрэглээгүй** — тэдгээр нь карт цээжлэхэд
зориулагдсан. Математикийн бодлогыг цээжилвэл утгагүй тул интервал нь тухайн
БОДЛОГОД биш, СЭДЭВт хамаарна.

⚠️ **Тест бодит алдаа барьсан:** `fitScore` анх УРВУУ байсан — сул сурагчид
хүнд бодлого санал болгож байв. `idealFor()` чиглэлийн хамгаалалт нэмсэн.

### 7.6 `Attempt.givenAnswer` — ML-ийн хамгийн чухал дохио

Өмнө нь зөвхөн `autoCorrect` (үнэн/худал) хадгалагддаг байсан — «буруу»
гэдгийг л хэлнэ, **«ЯАГААД буруу»** гэдгийг хэлэхгүй. Улмаас distractor
analysis, алдааны онош, ирээдүйн загвар сургалт бүгд боломжгүй байв.

Хадгалах утга: `gradeAnswer()`-ийн `canonicalAnswer` — сонголтын **жинхэнэ
текст**, холилтын дараалалаас ҮЛ ХАМААРНА. Түүхий индекс хадгалбал сессийн
`choiceOrder` байхгүй бол дараа нь тайлагдахгүй.

Сонирхолтой нь `gradeAnswer` энэ утгыг **аль хэдийн буцаадаг байсан** —
`finalize()` түүнийг зүгээр л хаяж байв.

### 7.7 Адаптив ЭЕШ бэлтгэлийн загвар (эзний зорилго)

**Сурагчийн өөрийн тэмдэглэгээ — 4 төлөв** (`SelfState`):
1. Бүрэн ойлгож, алдалгүй бодсон
2. Алдсан → засаад зөв гаргасан
3. Алдсан / чадаагүй
4. **Буудсан** (таамгаар сонгоод оноо авсан)

*Яагаад 4 дэх нь чухал вэ: сонголттой тестэд сурагчид буудаж оноо авдаг.
Өөрөөр тэмдэглүүлснээр **азын оноог жинхэнэ чадамжаас ялгана**.*

**Оролдлогын 4 суваг:** өдөр тутмын өөрийн тэмдэглэгээ · онлайн тест ·
сэдвийн бүлгийн шалгалт (багш оруулна) · OMR хуудас уншигч.

**Тооцооллын 3 үе:** (1) ил тод статистик — жинлэсэн дундаж + Elo маягийн
рейтинг → (2) IRT → (3) knowledge tracing (1 жилийн дата шаардана).
Систем таамаглалаа хадгалж, бодит дүнтэй харьцуулна.

### 7.8 SMS удирдлага

| Юу | Хаана |
|---|---|
| ӨС: `SmsMessage` · `SmsBatch` · `SmsTemplate` | `schema.prisma` |
| Бодит сервис (Prisma, rate limit, тооцоо) | `notifications/sms-management.service.ts` |
| HTTP гадаргуу | `notifications/sms.controller.ts` |
| SMS хэсгийн тоо | `notifications/sms-segments.ts` |
| Дэлгэц | `app/app/sms/`, `components/sms/` |

⚠️ **КИРИЛЛ БИЧВЭР 2 ДАХИН ҮНЭТЭЙ.** GSM-7 нь 160 тэмдэгт/хэсэг, кирилл нь
UCS-2 тул 70. Илгээхийн ӨМНӨ хэдэн хүн, хэдэн хэсэг, ойролцоо өртөг гэдгийг
харуулна — SMS буцаах боломжгүй.

⚠️ **НУУЦЛАЛ:** `PASSWORD_RESET` мессежийн `body`-д **КОД ХАДГАЛАХГҮЙ**.

### 7.9 Төлбөр — QPay

- Банкны **гадаад лого ХЭРЭГЛЭХГҮЙ** (§3.8). Оронд нь банкны нэр + нэрээс
  hash-аар гаргасан өнгөт товчлол, цэвэр CSS
- Гар утсан дээр deeplink эхэнд, ширээний компьютер дээр QR эхэнд
- Polling 5с, 5 минутын дараа зогсоно. Хуудас нуугдвал (document.hidden) зогсоно
- QR зураг нь base64 data URI — дотоод, зөв
- Идемпотент: `Payment.providerPaymentId @unique` — callback хоёр удаа ирэх,
  эсвэл callback + polling хоёулаа нэг гүйлгээг барих нь ХЭВИЙН тохиолдол
- Дэлгэрэнгүй: `api/src/gateways/README.md`, `web/src/components/checkout/README.md`

### 7.10 Бусад модуль (2026-08-08)

| Модуль | Юу |
|---|---|
| `insights/` | distractor analysis · бодлогын чанар (эвдэрсэн бодлого илрүүлэх) · сэдвийн эзэмшил · нэрээ хэшилсэн ML экспорт |
| `store/` | нэг удаагийн шалгалт, ном тусад нь зарах |
| `finance/` | орлого/зарлага, сарын тайлан, SQL-д нэгтгэсэн |
| `teacher-groups/` | гадны багшийн бүртгэл + бүлэг (`joinCode`-оор сурагч нэгдэнэ). Админ баталгаажуулах хүртэл хандахгүй |
| `tuition/` | төлбөрийн буцаалт |
| `recommend/` | бодлого санал болгох |

---

## 8. Цаг иддэг УРХИНУУД

Бүгд бодит алдаанаас гарсан. Дахин бүү ал.

### 8.1 ⚠️ `tsc` цэвэр байх нь ХАНГАЛТГҮЙ — гурван төрлийн алдаа мултардаг

**(а) `"use client"` дутуу** → tsc өнгөрч, **`npm run build` УНАНА**.
`useState`/`onClick` ашигладаг компонент Server Component-оос импортлогдвол.

**(б) NestJS модулийн экспорт дутуу** → tsc, jest хоёулаа өнгөрч, **апп
боохдоо УНАНА**. Хамаарлын мод зөвхөн ажиллах үед байгуулагддаг; модулийн
метадата бол ердөө объект.
2026-08-08: `ScheduleModule` нь `ScheduleService`-ээ экспортлоогүйгээс прод
deploy чимээгүй унасан. Render хуучин instance-аа үлдээсэн тул API нь
HTTP 200 буцаасаар байв — гаднаас нь бүх зүйл зөв харагдана.

**(в) Давхар `api/` префикс** → `@Controller('api/finance')` +
`setGlobalPrefix('api')` = `/api/api/finance/…`. Гурван модулийн 23 маршрут
бүгд 404 болсон.

→ Хамгаалалт: §2-ын ачаалалтын шалгалтыг deploy-ийн өмнө ЗААВАЛ ажиллуул.

### 8.2 Агентын тайланг БҮҮ ИТГЭ

- 2026-08-03: 10 агент «бүх глиф сольсон» гэсэн атал **225 үлдсэн**
- 2026-08-08: 10 агентын **3 нь** «хийсэн» гэж мэдээлсэн атлаа файл нь ОГТ
  өөрчлөгдөөгүй байв. Нэг нь tsc/jest-ийн **хуурамч гаралт** хүртэл бичсэн
- Нэг агент **байхгүй багана** дээр индекс үүсгэх миграци бичсэн — прод API-г
  унагаах байсан
- Агентууд **emoji** оруулсан (💡📳🔇), **байхгүй компонент** зохиосон
  (`<Surface>`), **гараар өнгө** бичсэн (`bg-green-50`)

→ Агент бүрийн дараа `git status` / `git diff --stat` -ээр **бодит**
өөрчлөлтийг шалга. Агентын schema-д `git_proof` талбар шаардвал худал
тайлагнал эрс буурдаг.

### 8.3 Нэг фолдерт хоёр агент оруулж БОЛОХГҮЙ

2026-08-08: `sms:api` бодит сервис бичсэн, `sms:ui` тэр зуур `sms.service.ts`
дээр хоосон stub бичиж controller-ыг дарж устгасан. Үр дүнд SMS модуль
бүхэлдээ ажиллахгүй байв.

### 8.4 Шинэ модуль нэмэхэд ГИНЖ бүтэн эсэхийг мөрд

схем → сервис → controller → **`app.module.ts` бүртгэл** → маршрутын зам →
клиентийн дуудлага (`api()` ашигласан уу) → **талбарын нэр таарч байна уу** →
навигацид холбоос.

2026-08-08: вэб `text`/`to`/`page` илгээж байхад сервер `body`/`phone`/`skip`
хүлээж байв. `any` төрлөөс болж tsc барьсангүй — ажиллах үедээ л унах байсан.

`ActivityModule` нь импортлогдсон атлаа `imports:` массивт байхгүйгээс
`/api/activity/*` бүгд 404 байсан — статик шинжилгээ олоогүй, **зөвхөн апп
ажиллуулж байж олдсон**.

### 8.5 Бусад

| Урхи | Дэлгэрэнгүй |
|---|---|
| `nest build` → `dist/src/main.js` | `dist/main.js` БИШ. `prisma.config.ts` нь `src/`-ээс гадна байдгаас |
| Render Prisma | `?sslmode=require` ЗААВАЛ — эс бөгөөс «User was denied access» |
| Prisma JSON `null` ≠ SQL NULL | `choices: null` нь JSON `null` бичдэг. `WHERE choices::text='null'` |
| `eslint --fix` | type assertion хасдаг → tsc унана |
| `TestAccess` хүснэгт | `id` багана БАЙХГҮЙ — нийлмэл түлхүүртэй |
| `tsconfig.json` | `ignoreDeprecations: "6.0"` (хүчингүй) → 10/10 тест уначихсан |
| Хөтчийн scroll | `scroll-behavior: smooth` → `window.scrollTo(0,0)` ажиллахгүй. `behavior:'instant'` |
| Render үнэгүй багц | 15 мин ажилгүй бол унтдаг, эхний хүсэлт ~60с. `lib/api.ts` retry-тэй |

---

## 9. Нээлттэй эрсдэл ба дараагийн ажил

### 9.1 🔴 Яаралтай — эзний шийдвэр

| Юу | Хугацаа | Хэрэв хийхгүй бол |
|---|---|---|
| **Render Postgres төлбөртэй багц** | **2026-08-18** | Үнэгүй ӨС-ийн хугацаа дуусна. **359 сурагчийн бүртгэл устана** — сэргээх боломжгүй |
| **SMS нийлүүлэгч** (Twilio эсвэл дотоодын) | — | Нууц үг сэргээх бүхэлдээ ажиллахгүй (503) |
| **QPay merchant гэрээ** | — | Код бэлэн, зөвхөн түлхүүр дутуу |
| **Тогтвортой диск** (зураг хадгалах) | — | Redeploy бүрт байршуулсан зураг **устдаг** |

> Render-ийн үнэгүй web service дээр `persistent disk` дэмжигддэггүй.
> `render.yaml` дээрх `disk:` болон `UPLOAD_DIR` тайлбар болгосон —
> багцыг `starter` болгомогц тайлбарыг авахад идэвхжинэ.

### 9.2 🟡 Кодын ажил

| Юу | Тайлбар |
|---|---|
| Бодлого санал болгохыг UI болгох | Логик бэлэн (23 тест), API+UI хийгдсэн, нүдээр шалгаагүй |
| Дэлгүүрийн админ UI | Үнэ тогтоох хэсэг дутуу |
| Ачааллын тест (k6) | 2000 зэрэг хэрэглэгчийн зорилтыг батлаагүй |
| PgBouncer / олон instance | `DB_POOL_MAX=25` нь түр шийдэл. Тооцоо `render.yaml`-д |
| «Ачаалж байна…» цэвэрлэгээ | Хэсэгчлэн дуусаагүй |
| Хэрэглэгч архивлах | `archived` талбар байхгүй |
| Сурагчийг бөөнөөр импортлох UI | Одоо зөвхөн скриптээр |
| 8 шалгалтын бодлого сэргээгдээгүй | Эх PDF эвдэрсэн |
| ~166 lint анхааруулга | Ихэнх нь `react-hooks/set-state-in-effect`. CI унтраасан |

### 9.3 Контентын мэдэгдэж буй асуудал

- **1913/1916 бодлого placeholder choices-той** (`["A".."E"]`) — жинхэнэ
  хувилбарууд нь бодлогын текст дотор. `isPlaceholderChoices()` үүнийг
  зохицуулдаг (байрлал солихгүй), гэхдээ **бүрэн шийдэл биш**
- LaTeX импорт: 1817/1844 CHOICE бодлого (98.5%), хариултын түлхүүр 99.0%
  баталгаажсан. 13 бодлого REVIEW төлөвтэй

### 9.4 ⚪ Хойшлуулсан (эзний шийдвэр)

`studentCode`/`teacherCode` хоосон · `mustChangePassword` бэлэн ч идэвхгүй ·
`three`, `@react-three/*` хамаарал `package.json`-д үлдсэн (bundle-д БАЙХГҮЙ,
`hero3d/primitives.tsx` орфан) · `MathText`-ийн `toSuper` unicode дээд
индекс гаргадаг (6125 бодлогод нөлөөлөх тул нүдээр шалгаж байж солино)

---

## 10. Ажлын хэв маяг (эзний хүсэл)

- **Олон агент зэрэг** ажиллуулах. Файлын эзэмшлийг агент бүрд ЯГ зааж өгнө
- Опус төлөвлөж удирдана, Sonnet нарийн ажлыг хийнэ
- Судалгааг эх сурвалжтай нь хий — таамаглаж бичихгүй
- Ажил дуусмагц **өөрөө шалга** — агентын тайлан хангалтгүй
- Дутуу үлдсэнийг **ил хэл** — нуухгүй

---

## 11. Түүхийн хураангуй

| Огноо | Юу |
|---|---|
| 2026-06 | Төслийн эхлэл, анхны схем (`docs/archive/SPEC.md`) |
| 2026-07-02 | Аудит — 30 асуудал илрүүлсэн |
| 2026-07-07 | LaTeX импорт: 1817 бодлого, хариу 99.0% баталгаажсан |
| 2026-07-27 | Эрхийн загварын шийдвэр — контент TEACHER_PLUS-д |
| 2026-07-30 | Хуваарийн систем (77 мөр, 21 анги), прод deploy |
| 2026-08-03 | Дизайны системийн шилжилт (emoji→lucide, 123 хөрвүүлэлт) |
| 2026-08-08 | SMS удирдлага · ML дүн шинжилгээ · төлбөрийн буцаалт · дэлгүүр · санхүү · гадны багш. Тест 144→226. Баримт нэгтгэсэн |

Дэлгэрэнгүй: `git log` болон `docs/archive/`.
