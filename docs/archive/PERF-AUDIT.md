# PERF-AUDIT.md — 1000 сурагч зэрэг 100 минутын шалгалт өгөх сценарийн ачааллын аудит

**Огноо:** 2026-07-28
**Хамрах хүрээ:** `api/src` (READ-ONLY), `api/prisma/schema.prisma` (READ-ONLY). Энэ баримт бичигт код өөрчлөлт ОРООГҮЙ — зөвхөн хэмжилт, ажиглалт.
**Арга зүй:** grep + унших (`api/src/**/*.ts`), `psql -d pimn` дээрх `\d`/`EXPLAIN ANALYZE`, web клиентийн `web/src/app/app/tests/[id]/page.tsx` кодоос хугацааны тооцоо.

---

## 0. Товч дүгнэлт — эрэмбэлсэн хүснэгт (impact/effort)

| # | Олдвор | Нотолгоо | 1000 хэрэглэгчид үзүүлэх нөлөө | Хэмжээ (effort) |
|---|---|---|---|---|
| 1 | **ThrottlerModule 300 req/min/IP — `app.set('trust proxy', ...)` байхгүй** | `api/src/main.ts` (бүхэлдээ уншсан, `trust proxy` алга); `api/src/app.module.ts:47` `ThrottlerModule.forRoot([{ ttl: 60_000, limit: 300 }])`; `tests.controller.ts`-д `@Throttle` override алга | 🔴 ХАМГИЙН ӨНДӨР — доор §5.1-д тооцоолол | Бага (1-2 мөр) |
| 2 | **`finalize()`-д бодлого бүрт тусдаа UPDATE (N+1-адил, batch биш)** | `tests.service.ts:927-941` — `statUpdates.map(...)` → `$transaction([...])` дотор бодлого тутамд 1 тусдаа `problem.update` | 🟠 Дунд-Өндөр — экзамен ДУУСАХ мөчид 1000 × (up to 40) query бөөгнөрнө | Дунд (raw SQL эсвэл batched update руу шилжих) |
| 3 | **Prisma/pg pool sizing тохируулаагүй** (аль хэдийн тэмдэглэгдсэн) | `prisma.service.ts:12` `new PrismaPg({ connectionString })` — `connection_limit`/`pgbouncer` алга (`.env`-д баталгаажуулав) | 🟠 Дунд — autosave-ийн тогтмол ~50 req/s + start/submit бөөгнөрөлтэй давхцвал queueing үүсэх эрсдэлтэй | Бага (нэг env/config мөр) |
| 4 | **Compression middleware байхгүй** | `package.json`-д `compression` пакет алга, `main.ts`-д ашиглаагүй | 🟡 Бага — autosave payload жижиг (§4), гэхдээ `start()`/teacher жагсаалт payload томрох тусам холбогдоно | Бага |
| 5 | **`start()`/`submit()` бөөгнөрөл — `loadTestWithProblems()` хүнд query 1000 удаа зэрэг** | `tests.service.ts:319-329` (`start`), `tests.service.ts:484-503` (`submit`), `loadTestWithProblems` — `tests.service.ts:643-676` | 🟡 Дунд — index бүрхэгдсэн ч 1000 × (40 бодлого + choiceOptions + analysis) SELECT нэг доор ирвэл spike үүснэ | Хэмжилт хийгдсэн, засах шаардлагатай эсэхийг ачааллын тестээр баталгаажуулах хэрэгтэй |
| 6 | **`myStats()` UNBOUNDED — сурагчийн БҮХ Attempt-ийг take-гүй татна** | `attempts/attempts.service.ts:184-195` | 🟢 Бага (шалгалтын үеэр биш, гэхдээ Attempt хүснэгт цаг хугацаагаар томордог) | Бага (`take` эсвэл цаг хугацааны цонх нэмэх) |
| 7 | **Login-ийн bcrypt (`bcryptjs`, native биш) — шалгалтын ӨМНӨХ landslide** | `auth.service.ts:207` `bcrypt.compare`, `package.json:39` `bcryptjs` | 🟢 Бага-Дунд — шалгалт эхлэхийн өмнөх 5-10 минутад 1000 нэвтрэлт зэрэг ирвэл CPU дарамт | Хэмжих шаардлагатай (доор §5.4) |
| 8 | **`LearningEvent`-д илүүдэл дан `userId` индекс composite-ийн хажууд** | `schema.prisma:937,940` (`@@index([userId])` БА `@@index([userId, occurredAt])`) | 🟢 Маш бага — бичихэд бага зэрэг илүү overhead, унших query-д огт нөлөөгүй (composite нь prefix-ээр аль хэдийн бүрхдэг) | Маш бага (cleanup) |

---

## 1. 🔥 HOT PATH INVENTORY — 1000 сурагч, 100 минутын шалгалт

Эх сурвалж: `web/src/app/app/tests/[id]/page.tsx` — `SAVE_DEBOUNCE_MS = 1200`, `HEARTBEAT_MS = 20_000` (мөр ~78-80).

### 1.1 Шалгалт эхлэхээс өмнө (t = −10мин … 0)
- `POST /auth/login` — 1000 сурагч proctor-ийн заавраар ойролцоо цонход нэвтрэнэ.
  Тооцоо: 1000 хүсэлт / ~5 минут (300с) цонход жигд тархвал ≈ **3.3 req/s**; бодит амьдрал дээр сүүлийн 30-60 секундэд төвлөрдөг тул **17-33 req/s burst** боломжтой.
- `GET /tests/:id` (getOne, `tests.service.ts:253`) — мета мэдээлэл, хөнгөн (§4.1).
- `POST /tests/:id/start` (`tests.service.ts:319`) — session нэг л удаа үүсгэнэ, `loadTestWithProblems()` дуудна (§1.3-т дэлгэрэнгүй).
  Тооцоо: 1000 хүсэлт / нэг proctor-ийн "Эхэл" тушаалын дараах ~30-60с цонход → **17-33 req/s**-ийн хүнд SELECT.

### 1.2 Шалгалтын үеэр (t = 0 … 6000с, буюу 100 минут) — АЛЬ ХЭДИЙН ТООЦООЛОГДСОН
CLAUDE.md-д өгөгдсөн (дахин баталгаажуулав, кодоос):
```
HEARTBEAT_MS = 20_000  →  1000 сурагч / 20с = 50 req/s тогтмол доод хязгаар
SAVE_DEBOUNCE_MS = 1200 →  идэвхтэй бичиж буй сурагч тутамд дээд тал нь 1/1.2с = 0.83 req/s
```
- **Доод (baseline) хурд**: бүх 1000 сурагч идэвхгүй байсан ч heartbeat л ажиллана → **50 req/s** тогтмол PATCH `/tests/:id/session`.
- **Оргил (burst) хурд**: шалгалтын эхэн (1-5 минут) бүх сурагч эхний хэдэн асуултад хариулж байх үед олонх идэвхтэй → жишээ нь нэг ангийн 20-30 сурагч зэрэг товшвол тэдгээрийн debounce цонхнууд давхцаж болно. Хэт консерватив тооцоолвол (300/1000 сурагч нэгэн зэрэг идэвхтэй): 300 × 0.83 ≈ **250 req/s нэмэлт**, нийт ойролцоогоор **300 req/s** орчим PATCH.
- Энэ зам (`saveSession`) аль хэдийн хөнгөвчлөгдсөн (`loadTestProblemIds` зөвхөн `problemId` татна — `tests.service.ts:427-433`) — ДАХИН ЗАСАХ ШААРДЛАГАГҮЙ.

### 1.3 Шалгалт дуусах (t ≈ 6000с орчим)
- `POST /tests/:id/submit` эсвэл heartbeat-ийн үед `isExpired()` илэрсэнээр автомат `finalize()` (`tests.service.ts:454-462`, `822-945`).
  Хугацаа бүгд ижил `timeLimitMin`-тэй бөгөөд эхэлсэн цаг proctor-оор синхрончлогдсон тохиолдолд **дуусах мөч мөн синхрончлогдоно** → 1000 `finalize()` дуудлага хэдэн минутын дотор бөөгнөрч болно.
  Дуудлага бүрд:
  - `loadTestWithProblems()` (submit замд) — 40 бодлогын бүтэн текст/choices/correctAnswer/choiceOptions/analysis.
  - `$transaction([...])`: `testResult.upsert` + `attempt.createMany` + `testAttemptSession.update` + **бодлого тутамд тусдаа `problem.update`** (§0-ийн олдвор #2, доор §2.1-д дэлгэрэнгүй).
  - Тооцоо: 1000 сурагч × (3 тогтмол query + дунджаар ~30 хариулсан бодлого тутамд 1 UPDATE) ≈ **33,000 нэмэлт UPDATE** богино цонхонд (магадгүй хэдэн минут).

**Дүгнэлт:** Автосэйвийн тогтмол урсгал (50-300 req/s) аль хэдийн хөнгөвчлөгдсөн зам дээр сайн зохицуулагдана. Гэхдээ ГУРВАН БӨӨГНӨРӨЛ (login, start, finalize) нь "sustained req/s"-ээс тусдаа — тэд **синхрончлогдсон эхлэл/төгсгөлтэй** тул богино хугацаанд илүү хурц spike үүсгэдэг. Эдгээрийг ачааллын тестээр (жишээ: k6/autocannon-оор `start`-ыг зэрэг 1000 удаа дуудах) баталгаажуулахыг зөвлөж байна — энэ аудитад ийм ачааллын тест хийгээгүй болохыг ил тод хэлье (§6).

---

## 2. 🐌 N+1 БОЛОН UNBOUNDED QUERY ХЯНАЛТ

`grep -rn "findMany" api/src --include="*.ts"` (generated/spec хассан) = **112 дуудлага**. Бүгдийг мөр мөрөөр гүн шинжлээгүй (§6-д ил тод дурдав) — доор шалгалттай ШУУД холбоотой болон хамгийн ноцтой олдворуудыг гаргав.

### 2.1 Шалгалтын hot path — АЛЬ ХЭДИЙН ХӨНГӨВЧЛӨГДСӨН, ГЭХДЭЭ...

| Файл:Мөр | Query | Ангилал | Тайлбар |
|---|---|---|---|
| `tests.service.ts:428` `loadTestProblemIds` | `testProblem.findMany({where:{testId}, select:{problemId:true}})` | BOUNDED (testId-ээр PK-гоор хязгаарлагдсан, ~40 мөр) | ✅ Аль хэдийн засагдсан hot path (autosave) |
| `tests.service.ts:927-941` `finalize` дотор | `statUpdates.map(...) → prisma.problem.update(...)` | **N+1 маягийн — бодлого тутамд тусдаа UPDATE, ЦИКЛД (`.map`) байгаа** | ❌ ШИНЭ ОЛДВОР. `$transaction([...])` массив нь Prisma-д "batched" боловч ЭЦЭСТЭЭ тус бүрдээ тусдаа SQL statement (нэг transaction дотор дараалан). 1000 сурагч × ~30 бодлого = мянга мянган нэмэлт UPDATE submit/finalize бөөгнөрөлд (§1.3). Bulk update (`UPDATE ... FROM (VALUES ...)` эсвэл `$executeRaw` CASE WHEN) болгож нэгтгэвэл нэг query болгож болно. |
| `tests.service.ts:643-676` `loadTestWithProblems` | `test.findUnique({include: {problems: {include: {problem: {select: {..., choiceOptions:{select:...}, analysis:{select:...}}}}}}})` | BOUNDED (нэг тестийн 40 бодлого) — гэхдээ ХҮНД select (statementText, choices, correctAnswer, бүх choiceOptions, analysis) | ✅ Session `start`/`submit`-д зөвхөн 1-2 удаа/сурагч дуудагдана (аль хэдийн зөв architecture) — §1.3-ын бөөгнөрлийн тооцоонд орсон болно |

### 2.2 Бусад модулиуд — admin/browsing дэлгэц (ШАЛГАЛТТАЙ ЗЭРЭГ АЖИЛЛАХГҮЙ)

| Файл:Мөр | Ангилал | Reachability |
|---|---|---|
| `attempts/attempts.service.ts:184-195` `myStats` | **UNBOUNDED** — `take` алга, сурагчийн БҮХ Attempt амьдралын туршид татагдана + nested `problem.tags.tag` | Сурагч "Миний сул талууд" хуудсыг үзэх үед — шалгалттай ЗЭРЭГ давхцахгүй ч Attempt хүснэгт цаг хугацаагаар томорно (одоо dev DB-д ердөө 50 мөр — production-д жилээр хуримтлагдана) |
| `attendance/attendance.service.ts:126-217` `byClassAndDate` дотор `for` цикл | Цикл JS-ийн дотор л (Map бөглөх), DB query биш — **N+1 БИШ**, зөв хэрэгжсэн | Ангийн ирц — нэг ангийн хэмжээнд (20-30 мөр) хязгаарлагдмал |
| `payments/payments.service.ts:384-393` `reverse` дотор `for` цикл | Цикл дотор `audit.record()` дуудна — DB write, гэхдээ revoke хийгдсэн pass-уудын тоогоор (ихэвчлэн 1) хязгаарлагдана | Админ/санхүүгийн дэлгэц, шалгалттай холбоогүй |
| `tasks/tasks.service.ts:326-331` | JS дотор Map угсрах цикл, DB query биш | Ажилтны төлөвлөгч — 16 ажилтан, шалгалттай холбоогүй |
| `common/access.ts:54-76` `collectPassScope` | BOUNDED-ойролцоо (нэг хэрэглэгчийн идэвхтэй pass, ихэвчлэн 1-3 мөр), гэхдээ `hasCoveringPass` нь `assertStudentAccess`-аас зөвхөн enrollment ОЛДООГҮЙ үед л дуудагдана | `start()`/`submit()`-д 1 удаа/сурагч, autosave-д ОГТ ороогүй (зөв) |

**Ил тод хэлэх зүйл:** 112 `findMany`-гийн дийлэнх нь (schedule, content, videos, lessons, colortags, classrooms, announcements, leads, tasks) багш/админ-ийн дэлгэц эсвэл нэг ангийн/номын хэмжээнд байгалиараа хязгаарлагдсан жагсаалт (10-100 мөр) — эдгээрийг мөр мөрөөр бүгдийг гүн шинжлэх цаг хугацаа энэ дугуйланд хүрэлцээгүй тул зөвхөн exam-hot-path болон хамгийн эрсдэлтэй хэдхэн файлыг гүн уншсан.

---

## 3. 🔑 INDEX COVERAGE — `psql -d pimn` дээр баталгаажуулав

⚠️ **Чухал сануулга:** dev DB-д мэдээлэл ЦӨӨН (`User`=377, `TestAttemptSession`=6, `Attempt`=50, `LearningEvent`=0). Postgres-ийн query planner ийм жижиг хүснэгтэд ихэвчлэн **Seq Scan сонгоно ЗӨВ индекс байгаа ч гэсэн** — учир нь seq scan бага мөрийн хувьд бодитоор хямд. Тиймээс доорх `EXPLAIN`-ийн "Seq Scan" гэдэг нь prod-д (мянган-сая мөртэй үед) мөн Seq Scan сонгоно гэсэн үг БИШ — индекс байгаа эсэхийг л баталгаажуулсан, гүйцэтгэлийн бодит тоог биш.

| Хайлт (hot query) | Индекс `schema.prisma`-д | `\d` командаар баталгаажуулсан | `EXPLAIN` үр дүн (dev, өгөгдөл цөөн) |
|---|---|---|---|
| `TestAttemptSession` where `(testId, studentId)` | `@@unique([testId, studentId])` (мөр 620) | ✅ `TestAttemptSession_testId_studentId_key` UNIQUE btree | Seq Scan (зөвхөн 6 мөртэй тул planner индексийг хямдгүй гэж үзсэн; UNIQUE индекс prod-д ашиглагдана) |
| `Attempt` where `(studentId, occurredOn)` | `@@index([studentId, occurredOn])` (мөр 672) | ✅ `Attempt_studentId_occurredOn_idx` | Seq Scan (50 мөр — ижил шалтгаан) |
| `Attendance` where `(classroomId, date)` | `@@unique([classroomId, studentId, date])` (мөр 198) — `classroomId`+`date` prefix-ээр ажиллана | ✅ | **Index Scan** ашигласан (`Attendance_classroomId_studentId_date_key`) — жижиг ч гэсэн planner индексийг сонгосон |
| `Enrollment` where `(studentId, leftAt)` | `@@index([studentId, leftAt])` (мөр 173) | ✅ `Enrollment_studentId_leftAt_idx` | **Index Scan** ашигласан |
| `LearningEvent` where `(userId, occurredAt)` | `@@index([userId, occurredAt])` (мөр 940) + илүүдэл `@@index([userId])` (мөр 937) | ✅ Хоёул байна | **Index Scan** ашигласан, гэхдээ дан `userId` индексийг сонгож occurredAt-г `Filter`-ээр (Index Cond биш) хэрэгжүүлсэн — 0 мөртэй тул planner-д хамаагүй; composite индекс байгаа тул prod-д зөв сонголт хийгдэнэ гэж найдаж болно |
| `Test` жагсаалт (`_count`, access filter) | `@@index([deletedAt])` (мөр 566) л байна; `chapter`/`access` filter-үүд FK индексээр (Prisma автоматаар FK-д индекс үүсгэдэггүй ГЭХДЭЭ энд chapter/access нь join tables, тэдгээрийн PK эхэлдэг `testId`-ээр) | ✅ `Test_deletedAt_idx` | Тест жагсаалт нь БАГШ-ийн дэлгэц (`tests.service.ts:186`) эсвэл сурагчийн `list()` (мөр 220) — сурагчийн хувилбар нь `enrollment`+`collectPassScope` нэгтгэсэн `OR`-тэй WHERE, шалгалтын явцад ДАХИН дуудагдахгүй (зөвхөн тестийн жагсаалт үзэх үед) тул экзамен-hot биш |

**Дүгнэлт:** Шалгалттай шууд холбоотой БҮХ query (session lookup, attempt insert, enrollment check) зохих индексээр бүрхэгдсэн. `LearningEvent`-ийн илүүдэл дан индекс (§0 олдвор #8) бол цэвэрлэгээ хийж болох маленький зүйл — экзамен-hot биш учир priority бага.

---

## 4. 📦 PAYLOAD ХЭМЖЭЭ

Сервер ажиллуулж бодит хэмжээ хэмжих боломжгүй байсан тул (dev DB-д бодит тест-контент цөөхөн) `select`/`include` бүтцээс тооцоолов — **энэ хэсгийг бодит хэмжилтээр биш inference-ээр гаргасан гэдгийг ил тод хэлье**.

### 4.1 `GET /tests/:id` (getOne, сурагчийн салбар, `tests.service.ts:280-313`)
`select: {id, title, type, gradingMode, timeLimitMin, variantLabel, problemCount, totalPoints, myResult, sessionStatus}` — маш хөнгөн (~200-400 байт), payload асуудал биш.

### 4.2 `PATCH /tests/:id/session` (autosave, hot path)
Хариу: `{status, leaveCount, remainingSec}` (`tests.service.ts:476-480`) — маш жижиг (~50-100 байт). Хүсэлтийн бие: тухайн үед өөрчлөгдсөн хариултууд л (dirty diff, бүгд биш — `flushSave()`-ийн `answersSnapshot`, `web/.../page.tsx` мөр ~180) — сайн зохион байгуулалттай, payload талаас асуудалгүй.

### 4.3 `POST /tests/:id/start` — хамгийн том payload (нэг удаа/сурагч)
`buildProblemViews()` (`tests.service.ts:741-773`) бодлого бүрд `{id, format, statementText, imageKey, points, choiceMode, choices}` буцаана — 40 бодлого × (statementText ~200-500 тэмдэгт + choices массив) ≈ **20-40 KB/сурагч**. 1000 сурагч синхрон эхэлбэл сүлжээгээр **20-40 MB** гарна богино хугацаанд (§1.1-ийн burst-тэй давхцана). Энэ бол ХЭРЭГЦЭЭТЭЙ дата (сурагч бодлогоо харах ёстой) — асуудал нь давхардал биш, харин **compression байхгүй** (§0 олдвор #4) тул энэ хэмжээгээрээ л wire-ээр явна.

### 4.4 Багшийн `tests.service.ts:186-194` `list()` (TEACHER_ROLES)
`include: {_count, access: {include: {classroom}}, chapter: {select: {book}}}` — тестийн тоо × жижиг метадата, багшийн тоо цөөн (сая биш) тул асуудалгүй.

---

## 5. ⚙️ CONFIG GAPS

### 5.1 🚨 ThrottlerModule 300 req/min/IP — хамгийн өндөр эрсдэл

`app.module.ts:47`: `ThrottlerModule.forRoot([{ ttl: 60_000, limit: 300 }])`. `tests.controller.ts`-д `PATCH :id/session`-д `@Throttle` override алга (grep-ээр баталгаажуулав) → глобал 300/min/IP хамаарна.

**Тооцоолол (§1.2-ын тоонуудаас):**
- Нэг сурагчийн doод хязгаар: heartbeat 1/20с = **0.05 req/s** = **3 req/min**.
- 300 ÷ 3 = **100 сурагч** нэг IP-нээс зэрэг heartbeat илгээвэл л аль хэдийн лимитэд хүрнэ (идэвхтэй бичихгүй байсан ч).
- Сурагчид идэвхтэй бичиж байвал debounce max 0.83 req/s = **50 req/min/сурагч** → зөвхөн **6 сурагч** нэг IP-ээс идэвхтэй бичвэл 300/min-д хүрнэ.
- Нэг ФИЗИК анги (сургалтын төвийн бодит тохиолдол — CLAUDE.md-д "359 сурагч ОДОО ажиллаж байгаа" гэсэн) ихэвчлэн НЭГ WiFi/NAT дундаа хуваалцдаг тул **20-30 сурагч НЭГ IP**-тэй тохиолдол ердийн зүйл. Энэ тохиолдолд throttle **ГАРАНТИТАЙ** тэсрэнэ (20 × 50/min = 1000/min >> 300 лимит).
- ⚠️ **ИЛҮҮ НОЦТОЙ БОЛОМЖИТ СЦЕНАРИ:** `main.ts`-д `app.set('trust proxy', ...)` огт байхгүй. Render/Cloudflare-ийн ард Express анхдагчаар `req.ip`-г шууд холбогдсон peer-ээс авдаг (Render-ийн дотоод load balancer/proxy) — хэрэв энэ нь БҮХ гадаад хүсэлтэд НЭГ ижил дотоод IP харуулж байвал (`X-Forwarded-For` итгэмжлэгдээгүй тул) ThrottlerGuard-ийн `req.ip`-д суурилсан tracker БҮХ хэрэглэгчийг НЭГ IP гэж үзэж, **бүх системийг 300 req/min-д хамгийн эхний секундүүдэд хаах эрсдэлтэй**. Энэ таамаглалыг би баталгаажуулах боломжгүй (Render-ийн бодит сүлжээний тохиргоог шалгах эрхгүй) — гэхдээ кодоос харагдаж буй баримт (`trust proxy` тохиргоогүй) нь бодит эрсдэл. **PROD ДЭЭР шалгах шаардлагатай: `curl -i https://<api>/api/health` дуудаад Render логоос `req.ip` ямар утга бичиж байгааг харах.**

**Санал:** (1) `app.set('trust proxy', 1)` (эсвэл Render/Cloudflare-ийн тодорхой тоогоор) нэмэх; (2) `/tests/:id/session`, `/tests/:id/start`, `/tests/:id/submit`-д `@Throttle` override-оор илүү өндөр (эсвэл throttle-гүй, учир нь эдгээр endpoint нь аль хэдийн JWT-ээр хэрэглэгч тус бүрээр танигдсан — IP биш userId-аар throttle хийвэл илүү зөв) лимит тавих. Энэ бол код өөрчлөлт тул миний хамрах хүрээнээс гадуур — дараагийн ажлын багт зөвлөмж болгон дамжуулна.

### 5.2 Prisma/pg pool sizing (аль хэдийн тэмдэглэгдсэн баримт, дахин баталгаажуулав)
`prisma.service.ts:12`: `new PrismaPg({ connectionString: process.env.DATABASE_URL })`. `.env`-ийн `DATABASE_URL` (dev): `postgresql://pi:***@localhost:5432/pimn?schema=public` — `connection_limit`, `pool_timeout`, `pgbouncer` параметр АЛГА. `node-postgres`-ийн `Pool` анхдагч `max` = 10 холболт. 1000 сурагчийн 50-300 req/s (§1.2) + start/submit бөөгнөрөл (§1.3) нь 10 холболтоор дамжих үед queueing (хүлээлт) үүсгэх магадлалтай. **Прод дээрх бодит тоог (Render-ийн Postgres plan-ийн max_connections) шалгаагүй тул яг ямар `connection_limit` тохиромжтойг тоолж чадахгүй байна — энэ бол честно хэлэх ёстой хязгаарлалт.**

### 5.3 NestJS платформ, compression, cache headers
- `package.json:34`: `@nestjs/platform-express` — Fastify БИШ Express хэрэглэж байна (`main.ts`-д `NestFactory.create(AppModule)` — platform заагаагүй тул Express анхдагч).
- `compression`, `helmet` пакет АЛГА (`package.json`-д байхгүй, `main.ts`-д `app.use(...)` ганц ч удаа алга).
- Public GET (жишээ: `EnrollmentWindow`, `Lead` бүртгэлийн маягт landing page-д ашигладаг endpoint-ууд) дээр HTTP cache header (`Cache-Control`) тавьсан газар олдсонгүй — гэхдээ эдгээр нь шалгалтын hot path биш тул priority бага, зөвхөн бүртгэгдсэн болгоно.

### 5.4 Login-ийн bcrypt (bcryptjs, native биш)
`package.json:39`: `bcryptjs@^3.0.3` (цэвэр JS хэрэгжилт, native `bcrypt`-ээс удаан ч TypeScript/зөөврийн давуутай). `auth.service.ts:207` `bcrypt.compare()` — async хувилбар нь `setImmediate`-ээр event loop-д "sugar" өгдөг тул бүрэн блоклохгүй ч CPU-Intensive хэвээр. Шалгалт эхлэхийн өмнөх 5-10 минутад 1000 нэвтрэлт зэрэг ирвэл (§1.1) нэг Node процесс дээр (Render-ийн нэг instance) энэ нь бусад хүсэлтийн (жишээ: `start()`) хариу өгөх хугацааг сунгаж болзошгүй. **Би энэ нөлөөг бодитоор бенчмарк хийж хэмжээгүй** (`bcrypt.compare` нэг дуудлагын дундаж хугацааг load test-ээр шалгах шаардлагатай) — зөвхөн эрсдэлийг тэмдэглэж байна.

---

## 6. ХЭМЖИЖ ЧАДААГҮЙ ЗҮЙЛС — ИЛ ТОД ХЭЛЬЕ

1. **Бодит ачааллын тест (load test) хийгдээгүй.** Бүх req/s тоо нь кодын дебоунс/heartbeat интервал + 1000 хэрэглэгчийн арифметик тооцооноос гарсан онолын тоо — бодит сервер дээр k6/autocannon-оор баталгаажуулаагүй.
2. **Prod DB (Render Postgres)-ийн `max_connections`, plan хэмжээ шалгаагүй** — `connection_limit`-ийг ямар тоонд тохируулах ёстойг зөвлөх боломжгүй, зөвхөн одоо тохируулаагүй байгааг баталгаажуулсан.
3. **112 `findMany`-гийн БҮГДийг мөр мөрөөр гүн шинжлээгүй** — зөвхөн exam-hot-path (`tests.service.ts`, `common/access.ts`) болон хэдэн сэжигтэй файлыг (`attempts.service.ts`, `attendance.service.ts`, `payments.service.ts`) гүн уншив. Бусад ~70 орчим дуудлага нь файлын нэр/контекстээс "жижиг, байгалиараа хязгаарлагдсан" гэж дүгнэсэн ч баталгаажуулаагүй.
4. **`Render`-ийн `trust proxy`/proxy сүлжээний бодит зан үйлийг шалгах эрхгүй** — §5.1-ийн хамгийн ноцтой (боловч батлагдаагүй) таамаглал бол prod дээр шууд шалгах хэрэгтэй зүйл.
5. **Payload хэмжээг сервер ажиллуулж бодитоор хэмжээгүй** (§4) — зөвхөн select бүтцээс тооцоолсон тоо.
6. **bcrypt-ийн бодит CPU зарцуулалтыг benchmark хийгээгүй** (§5.4).

---

## Дүгнэлт

Автосэйвийн үндсэн зам (`saveSession`) аль хэдийн зөв хөнгөвчлөгдсөн, индекс бүрхэлт (unique/composite) шалгалтын hot path-д бүрэн тохирсон байна — энэ хоёр том асуудал ЭНЭ АУДИТАД шинээр ОЛДООГҮЙ (аль хэдийн засагдсан/зөв хийгдсэн).

Хамгийн өндөр эрсдэл нь **код доторх query биш, харин ирж буй хүсэлтийг зогсоох давхарга (ThrottlerModule + trust proxy)** — учир нь энэ нь бүх бусад сайн хийгдсэн оптимизацийг үл харгалзан 300 req/min дээр бүх (эсвэл нэг ангийн) сурагчийг өөрсдөө хааж, "self-inflicted outage" үүсгэх боломжтой. Хоёрдугаарт, `finalize()`-ийн бодлого тутамд тусдаа UPDATE нь шалгалт дуусах мөчид (синхрончлогдсон бол) мянга мянган нэмэлт query үүсгэнэ. Эдгээр хоёрыг эрэмбийн тэргүүнд тавихыг санал болгож байна.

⚠️ **ШИНЭЧЛЭЛТ (доор §7):** §5.1-д дурдсан `trust proxy` болон userId-аар key хийдэг Throttler-ийг өөр агент аль хэдийн ЗАСЧЭЭ (`main.ts:20`, `app.module.ts:58-87` — энэ баримт бичиг анх бичигдэх үед хараахан засагдаагүй байсан тул дээрх §5.1-ийн тайлбар "ОДООГИЙН биш ТҮҮХЭН" болсон гэдгийг тэмдэглэе). §7-д ЭНЭ шинэчлэгдсэн код дээр бодит ачааллын тест хийсэн үр дүнг оруулав.

---

## 7. 🧪 RESULTS — бодит ачааллын тестийн хэмжилт (localhost)

**Хэрэгсэл:** `api/test/load/` (шинэ npm dependency АВААГҮЙ — Node.js built-in `http`/`crypto`-оор бичигдсэн, `package.json`-д `loadtest:exam` гэсэн ГАНЦ script нэмэгдсэн). Ажиллуулах: `npm run loadtest:exam` (`api/`-ийн дотроос).

**Аюулгүй байдал:** `test/load/lib/env.js` нь `DATABASE_URL` БОЛОН зорилтот API host аль алиныг нь `localhost`/`127.0.0.1` эсэхийг шалгаж, эсрэг тохиолдолд ШУУД throw хийж зогсоодог хатуу хамгаалалттай (кодоор баталгаажуулсан, зөвхөн баримтжуулаагүй). Тест бүрийн эцэст (`finally` блок) фикстур (сурагч/анги/тест/бодлого) DB-ээс бүрэн устгагдсаныг ажиллуулсан бүрд `prisma.count()`-оор баталгаажуулсан — 0 мөр үлдсэн.

**Арга зүй:** N виртуал сурагч тус бүр `POST start → давтан PATCH session (autosave) → POST submit` гүйцэтгэнэ. JWT-г `JWT_SECRET`-ээр аппын адилхан (HS256) гарын үсэг зурж мингнэ — `/auth/login`-г тойрсонгүй, зөвхөн bcrypt login алхмыг алгассан (тест бүрд login хийвэл 1000+ bcrypt.compare зэрэг нэмэлт хувьсагч оруулах байсан тул тусад нь §7.4-д авч үзсэн). **100 минутын хугацааг ШАХСАН**: жинхэнэ ~300 autosave (1.2с debounce)-ыг сурагч тутамд 8 autosave × 200мс зайтай болгож багасгав — доор §7.4-д энэ хязгаарлалтын үр дагаврыг ил тод тайлбарлав.

### 7.1 Хувилбар A — БОДИТ (тархсан ирэлттэй) ачаалал — 50 → 1000

Сурагчид `arriveSpread` (3с → 15с, N-тэй хамт өсдөг) хугацаанд тарж `start` дуудна — PERF-AUDIT §1.1-ийн "1000 хүсэлт 17-33 req/s burst" тооцоог ойролцоолсон бодит хувилбар.

| N | req/s | start p50/p95/p99 | session p50/p95/p99 | submit p50/p95/p99 | Алдаа |
|---|---|---|---|---|---|
| 50 | 108.8 | 7.9 / 16 / 57.7 ms | 3.5 / 7 / 13.9 ms | 12.1 / 16.8 / 27.3 ms | 0% |
| 200 | 432.2 | 5.1 / 8.3 / 10.2 ms | 1.6 / 2.9 / 3.9 ms | 8.8 / 11.3 / 15.1 ms | 0% |
| 500 | 547.8 | 4.4 / 8.2 / 13.1 ms | 1.5 / 2.8 / 6.1 ms | 8.6 / 11.4 / 19.2 ms | 0% |
| **1000** | **600.9** | 4.8 / 12.4 / 62.2 ms | 1.9 / 7.5 / 30.6 ms | 9.3 / 22.7 / 73.1 ms | **0%** |

**N=1000-д бүх 10,000 хүсэлт (start+session×8+submit) АМЖИЛТТАЙ, p99 бүх endpoint-д 100мс-ээс бага.** Энэ бол зорилтот сценарийн бодит ойролцоо туршилт — `trust proxy`/userId-throttler засварын дараа энэ хэсэгт `429` НЭГ Ч удаа гараагүй.

### 7.2 Хувилбар B — ХАМГИЙН МУУ тохиолдол: 0 тархалттай (бүгд яг НЭГ мөчид) burst — 1000 → 3000

`arriveSpread=0` — БҮХ N сурагч яг НЭГ зэрэг `start` дуудна. Энэ нь PERF-AUDIT §1.1-ийн ("proctor-ийн заавраар ойролцоо цонход") өөрийн тооцооноос ХАМААГҮЙ хатуу — real world-д ийм зэрэгцээ (0мс jitter) бараг тохиолдохгүй, гэхдээ "хамгийн муу тохиолдол"-ыг олохын тулд зориудаар туршив.

| N | req/s | start p50/p95/p99/max | session p50/p95/p99 | submit p50/p95/p99 | Алдаа |
|---|---|---|---|---|---|
| **1000** | 1050.1 | 1636 / 5448 / **6951** / 6953 ms | 253 / 420 / 451 ms | 802 / 1021 / 1066 ms | **0%** |
| 2000 | 756.5 | 3915 / 4298 / 4395 ms | 808 / 1126 / 1357 ms | 9135 / 11574 / 12333 ms | **4.46%** (893/2000 submit → HTTP 500) |
| 3000 | 676.8 | 5326 / 7396 / 8652 ms (+18 клиент timeout) | 1861 / 11396 / 12777 ms (2858 → HTTP 500) | зөвхөн 124/3000 хүрсэн | **14.73%** |

**Дүгнэлт:** N=1000 дээр Ч ГЭСЭН (хамгийн муу боломжит нөхцөлд) алдаа 0% — гэхдээ `start()`-ийн p99 ~7 секунд хүрдэг (сурагч "Эхлэх" дараад 7 сек хүлээх болзошгүй, гэхдээ АЛДАА БИШ, зөвхөн удаан). **N=2000-оос эхлээд бодит алдаа (HTTP 500) гарч эхэлдэг — энэ бол localhost дээр олдсон бодит "эвдрэх цэг" (breaking point).** 1000 бол зорилтот тоо тул N=2000-3000-ийн эвдрэлт ЗОРИЛТООС 2-3 дахин дээгүүр байгааг тэмдэглэе — нөөц зай бий, гэхдээ хязгааргүй биш.

### 7.3 Pool sizing эксперимент — `DB_POOL_MAX` нэмэгдүүлбэл засах уу?

PERF-AUDIT §5.2/§0-#3 "pool sizing тохируулаагүй" гэж эргэлзсэн тул ЯГ Хувилбар B-ийн (0 тархалттай burst) нөхцөлд `DB_POOL_MAX=15` (`prisma.service.ts`-ийн анхдагч) ба `DB_POOL_MAX=40`-ийг ШУУД харьцуулав:

| N | DB_POOL_MAX | req/s | start p99 | Алдаа |
|---|---|---|---|---|
| 1000 | 15 (анхдагч) | 1050.1 | 6951 ms | 0% |
| 1000 | 40 | 653.9 | **7227 ms** (муудсан) | 0% |
| 2000 | 15 (анхдагч) | 756.5 | 4395 ms | 4.46% |
| 2000 | 40 | 312.0 | **8703 ms** (муудсан) | **11.03%** (муудсан) |

**Чухал, гэхдээ сөрөг үр дүн: pool-ийг 15→40 болгож ИХЭСГЭХ нь ТУСГААГҮЙ, харин ХОЁУЛАА (латенси, алдааны хувь) МУУДСАН.** Энэ нь `finalize/start`-ийн 0-jitter burst-ийн эвдрэлт **Postgres connection pool-ийн ХЯЗГААРЛАЛТААС биш** гэдгийг илтгэнэ — учир нь pool-ийг өргөтгөх нь тусалсангүй (харин Postgres/Node дээр НЭМЭЛТ зэрэгцээ ажил үүсгэж дарамтыг нэмэгдүүлсэн байж болзошгүй). Иймд **CLAUDE.md-ийн "premature infrastructure нэмэхгүй" зарчмыг баталгаажуулж байна** — pool томруулах, Redis/queue нэмэх зэрэг нь ЭНЭ хэмжилтээр батлагдаагүй бөгөөд магадгүй нөхцөлийг МУУДГАНА. Одоогийн `DB_POOL_MAX=15` анхдагчийг өөрчлөх шаардлагагүй гэж дүгнэж байна (доор §7.5-д бодит хязгаарлагч хүчин зүйлийн таамаглал).

### 7.4 ИЛ ТОД ХЭЛЬЕ — энэ хэмжилт юуг батлаж, юуг БАТЛАХГҮЙ БАЙНА

1. **Клиент БА сервер ХОЁУЛАА НЭГ л машин дээр ажилласан.** Ачааллын скрипт (Node) болон API сервер (мөн Node) яг ижил CPU/цөмүүдийг хуваалцсан тул §7.2/§7.3-ийн ӨНДӨР латенси/алдааны тоо нь ЗӨВХӨН серверийн ачаалал БИШ, ХЭСЭГЧИЛЭН клиент талын өрсөлдөөнийг (1000+ зэрэгцээ HMAC гарын үсэг, JSON encode, HTTP socket удирдлага) ХАМТАД нь тусгасан байж болзошгүй. Бодит production-д 1000 сурагч 1000 ӨӨР төхөөрөмж дээрээс ирдэг тул серверт ийм "хосолсон" дарамт үгүй. **Энэ хязгаарлалтаас болж §7.2/7.3-ийн тоог "серверийн цэвэр багтаамж" гэж шууд уншиж болохгүй** — харин "нэг машин дээрх хосолсон системийн доод хязгаар" гэж ойлгох ёстой (өөрөөр хэлбэл ЖИНХЭНЭ серверийн багтаамж үүнээс ДЭЭГҮҮР байх магадлалтай). §7.1 (тархсан ирэлт)-ийн тоонууд энэ асуудалд бага өртөмтгий (клиент дарамт бага, тархсан) тул илүү найдвартай.
2. **Хугацаа ШАХСАН (compressed).** Сурагч тутамд 8 autosave (жинхэнэ ~300-ын оронд), 200мс зайтай (жинхэнэ 1200мс debounce-ийн оронд). Энэ нь **`session` endpoint-ийн НЭГ хүсэлтийн боловсруулах зардлыг олон зэрэгцээ холболтод хэмжихэд хангалттай**, ГЭХДЭЭ **100 минутын турш үргэлжлэх зан төлөвийг (санах ойн алдагдал, log/файлын хэмжээ өсөлт, DB холболтын урт хугацааны элэгдэл) огт хэмжсэнгүй**.
3. **Dev DB бараг хоосон** (§3-т аль хэдийн тэмдэглэсэн — `User`≈377, бусад хүснэгт цөөн). Postgres query planner том хүснэгттэй prod-д өөр төлөвлөгөө (жишээ: Index Scan vs Seq Scan) сонгож болзошгүй тул **энд хэмжсэн абсолют query хугацаа prod дээр ижил байх баталгаа алга**.
4. **Localhost — Render-ийн сүлжээ/hardware огт биш.** Cloudflare hop, TLS handshake, Render-ийн дотоод load balancer, жинхэнэ хэрэглэгчийн интернэтийн латенси (localhost-д 0мс) — эдгээр БҮГД ЭНД байхгүй. Render instance-ийн CPU тоо/санах ой, Render Postgres-ийн `max_connections`/plan хэмжээг би ШАЛГАХ ЭРХГҮЙ (§6-д аль хэдийн тэмдэглэсэн). **Иймд §7-ийн req/s тоог "Render дээр ХҮЛЭЭГДЭХ req/s" гэж шууд шилжүүлж болохгүй** — зөвхөн КОДЫН ЗАМ (logic, cascade, throttling, grading, Prisma query бүтэц) 1000-3000 зэрэгцээ session-д ЛОГИК АЛДАА/DEADLOCK/DATA CORRUPTION-гүйгээр ажилладгийг батална.
5. **Login (bcrypt) endpoint-ийг ЭНЭ ачааллын тестэд ОРУУЛААГҮЙ** — JWT-г шууд гарын үсэг зурсан (§7 эхэнд тайлбарласан). PERF-AUDIT §5.4-ийн "bcrypt CPU зарцуулалт" асуулт ХЭВЭЭР хариулагдаагүй хэвээр байна.

### 7.5 ХОНЕСТ ВЕРДИКТ

**Тийм, ЭНЭ хэмжилтээр 1000 сурагч зэрэг 100 минутын шалгалт өгөх БОДИТ (тархсан ирэлттэй, PERF-AUDIT §1.1-ийн тооцоотой нийцсэн) сценар дор одоогийн код 0% алдаатай, бүх endpoint-д p99 <100мс-ээр ажилласан** (§7.1). `trust proxy`/userId-throttler засвар (§5.1-д дурдсан асуудал ӨӨР агентаар засагдсан) энэ тестэд НЭГ Ч удаа throttle хийгдээгүй нь баталгаажлаа.

**Гэхдээ** зорилтот тоо (1000) яг л ХЯЗГААР дээр байгаа нь ЭНЭ хэмжилтээр ажиглагдав: хамгийн муу боломжит нөхцөлд (0 jitter, бүгд яг нэг мөчид) N=1000 ХЭВЭЭР 0% алдаатай (§7.2) ч `start()`-ийн p99 аль хэдийн ~7 секунд хүрч, N=2000-оос HTTP 500 бодитоор эхэлж байна — өөрөөр хэлбэл **нөөц зай (margin) байгаа ч том биш** (localhost дээр ~2x).

**Хязгаарлагч хүчин зүйл (§7.3-ийн туршилтаар баталгаажсан):** Postgres connection pool БИШ (pool 15→40 болгоход ТУСЛААГҮЙ, харин муудсан). Магадлалтай тайлбар: (a) `start()`/`submit()`-ийн `loadTestWithProblems()` — 40 бодлогын бүтэн текст+choices+choiceOptions+analysis татаж, JSON болгож сериализуулах ажил (PERF-AUDIT §4.3-т аль хэдийн "20-40KB/сурагч" гэж тооцоолсон) НЭГ зэрэг 1000+ удаа явахад Node-ийн ганц event loop/CPU бөглөрдөг байж болзошгүй; (b) Postgres өөрөө 1000+ зэрэгцээ дундаж хүнд join query-г нэг зэрэг гүйцэтгэхэд CPU-гаараа хязгаарлагдаж болзошгүй. Аль нь давамгайлж байгааг ЭНЭ хэмжилтээр бүрэн ялгаж чадаагүй (§7.4-#1-ийн клиент/сервер хосолсон дарамтын хязгаарлалтаас шалтгаалан) — CPU profiling (`node --prof` эсвэл `clinic.js`) хийж `loadTestWithProblems()`-ийн бодит зардлыг тусгаарлаж хэмжвэл дараагийн алхам байх болно.

**Санал болгох дараагийн алхам (код өөрчлөлт ОРООГҮЙ, зөвхөн зөвлөмж):**
- `start()`/`submit()`-ийн `loadTestWithProblems()`-г хөнгөвчлөх (`saveSession`-д аль хэдийн хийсэнтэй адил зарчмаар — зөвхөн шаардлагатай талбарыг л сонгох, эсвэл choiceOptions/analysis-ыг тусад нь lazy ачаалах) — ЭНЭ бол §7.2/§7.5-ийн цорын ганц удаагийн (once-per-student) хамгийн хүнд query.
- Redis/queue/pool-ийг ИХЭСГЭХГҮЙ (§7.3-аар батлагдсанаар туслахгүй, магадгүй муудна) — CLAUDE.md-ийн зарчимтай нийцнэ.
- Хэрэв Render дээр бодитоор шалгах бол: staging instance дээр ӨӨР МАШИНААС (localhost биш) ижил `test/load/index.js`-ийг `LOADTEST_API_BASE`-ээр чиглүүлж ажиллуулах (⚠️ ЗӨВХӨН DATABASE_URL мөн localhost/staging руу заавал заасан тохиолдолд — `lib/env.js`-ийн хамгаалалтыг зориудаар өөрчлөхгүйгээр prod руу АЖИЛЛАХГҮЙ гэдгийг сануулъя).
