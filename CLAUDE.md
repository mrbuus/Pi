# Pi.mn

**Ажил эхлэхээсээ өмнө `STATUS.md`-ыг УНШ.** Тэнд төслийн одоогийн байдал,
эзний хатуу дүрмүүд, deploy-ийн онцлог, цаг иддэг урхинууд бүгд бий.
Түүнийг уншсаны дараа кодыг дахин судлах шаардлагагүй.

UI код бичих бол нэмээд `web/DESIGN.md`.

## Хамгийн чухал 5 зүйл (дэлгэрэнгүйг STATUS.md-аас)

1. Математик бичвэр ҮРГЭЛЖ LaTeX (`MathText`). `π`, `²` гэх мэт энгийн
   тэмдэгт хориотой.
2. Emoji болон unicode глиф хориотой — зөвхөн `lucide-react`.
3. **Vercel автоматаар deploy хийдэггүй** — `cd web && npx vercel@latest
   --prod --yes` гараар. Render (API) нь автомат.
4. Гарагийн дугаарлалт: эзэн МОНГОЛ (1=Даваа…7=Ням), ӨС нь JS
   (0=Ням…6=Бямба). Андуурвал бүх хуваарь нэг өдрөөр гулсана.
5. Хэрэглэгчийн нууц үг = утасны дугаар (эзний дүрэм).

## Шалгалт

```bash
cd api && npx tsc --noEmit && npx jest      # 226/226
cd web && npx tsc --noEmit && npm run build  # ⚠️ tsc ХАНГАЛТГҮЙ — build ЗААВАЛ
                                             #    (use client дутуу байвал
                                             #     зөвхөн build барьдаг)

# ⚠️ DEPLOY ХИЙХЭЭС ӨМНӨ ЗААВАЛ — апп БООЖ байгааг шалга.
# NestJS-ийн хамаарал зөвхөн АЖИЛЛАХ ҮЕД шийдэгддэг тул tsc ба jest
# хоёулаа модуль экспортлоогүйг БАРЬДАГГҮЙ. 2026-08-08-нд ScheduleModule
# нь ScheduleService-ээ экспортлоогүйгээс прод deploy чимээгүй унасан.
cd api && npm run build && node -e "setTimeout(()=>process.exit(0),15000); require('./dist/src/main')" 2>&1 | grep -c "Nest can\'t resolve"
# → 0 байх ЁСТОЙ
```
