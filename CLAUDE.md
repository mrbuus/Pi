# Pi.mn

**Ажил эхлэхээсээ өмнө `STATUS.md`-ыг УНШ.** Тэнд төслийн бүх мэдлэг бий:
одоогийн байдал, эзний хатуу дүрэм, **дизайны гэрээ**, цаг иддэг урхи,
нээлттэй эрсдэл. Уншсаны дараа кодыг дахин судлах шаардлагагүй.

`STATUS.md` бол **цорын ганц эх сурвалж**. Хуучин баримтууд `docs/archive/`-д
байгаа — лавлагаа болгон уншиж болно, гэхдээ зөрчилдвөл `STATUS.md` давамгайлна.

## Хамгийн чухал 6 зүйл (дэлгэрэнгүйг STATUS.md-аас)

1. Математик бичвэр ҮРГЭЛЖ LaTeX (`MathText`). `π`, `²`, `·` хориотой.
2. Emoji болон unicode глиф хориотой — зөвхөн `lucide-react`.
   Тусгаарлагч цэгийг `components/ui/Meta.tsx`-ээс (`·` гараар бичихгүй).
3. **Vercel автоматаар deploy хийдэггүй** — гараар (`STATUS.md` §4).
   Render (API) нь автомат.
4. Гараг: эзэн МОНГОЛ (1=Даваа…7=Ням), ӨС нь JS (0=Ням…6=Бямба).
5. Хэрэглэгчийн нууц үг = утасны дугаар (эзний дүрэм).
6. Түүхий `fetch("/api/…")` бичихгүй — `lib/api.ts`-ийн `api()` ашигла.
   Харьцангуй зам прод дээр 404 болно (вэб Vercel, API Render дээр).

## Шалгалт

```bash
cd api && npx tsc --noEmit && npx jest        # 235/235
cd web && npx tsc --noEmit && npm run build   # build ЗААВАЛ
cd api && npm run smoke                       # бүх endpoint бодит HTTP-ээр (:3000 асаалттай үед)
```

**`tsc` цэвэр байх нь ХАНГАЛТГҮЙ.** Гурван төрлийн алдаа зөвхөн build эсвэл
апп боохдоо илэрдэг: `"use client"` дутуу · NestJS модулийн экспорт дутуу ·
давхар `api/` префикс.

Deploy-ийн өмнө **ачаалалтын шалгалтыг ЗААВАЛ** ажиллуул — `STATUS.md` §2.
