# Pi.mn — Production байршуулалтын гарын авлага

> Сүүлд шинэчилсэн: 2026-07-27
> Одоогийн топологи: **API + Postgres → Render** · **Web → Vercel**

---

## ⚠️ Яг одоо байгаа 3 эрсдэл

| # | Эрсдэл | Үр дагавар | Шийдэл |
|---|---|---|---|
| 1 | **Postgres `free` багц** | **30 хоногийн дараа өгөгдлийн сан УСТАНА** — 359 сурагчийн бүртгэл, бүх дүн алга болно | Багц сайжруулах (доор) |
| 2 | **Тогтвортой диск байхгүй** | Байршуулсан зураг/PDF **redeploy бүрт устдаг** | Disk холбох (доор) |
| 3 | **Web сервис `free`** | Идэвхгүй үед унтарч, эхний хүсэлт 30+ секунд болдог | `starter` руу сайжруулах |

> 💡 Эдгээр нь кодын алдаа биш — **багцын хязгаарлалт**. Код бүрэн бэлэн болсон.

---

## 1️⃣ Render багц сайжруулах (таны хийх ганц гар ажил)

Би төлбөрийн үйлдэл хийж чадахгүй тул энэ алхмыг та өөрөө хийнэ. Бусад бүх бэлтгэл дууссан.

### Postgres

1. [dashboard.render.com](https://dashboard.render.com) → **pimn-db**
2. **Settings → Plan → Change Plan**
3. **Basic-256mb** сонгоно (хамгийн хямд төлбөртэй сонголт, устдаггүй)

> ⏰ **Яаралтай:** үнэгүй багц үүсгэснээс хойш 30 хоногт устдаг.
> Устахаас өмнө сайжруулбал өгөгдөл бүрэн хадгалагдана.

### Web сервис + диск

1. **pimn-api** → **Settings → Plan** → **Starter**
2. Дараа нь **Disks → Add Disk**:
   - Name: `pimn-uploads`
   - Mount path: `/var/data/uploads`
   - Size: `5 GB`

> `render.yaml`-д disk болон `UPLOAD_DIR` аль хэдийн бичигдсэн — багцаа сайжруулмагц
> автоматаар үйлчилнэ. Код өөрчлөх шаардлагагүй.

---

## 2️⃣ Аль хэдийн зассан зүйлс ✅

| Юу | Өмнө | Одоо |
|---|---|---|
| `WEB_ORIGIN` | `https://placeholder.vercel.app` ❌ | `https://web-one-pi-59.vercel.app` ✅ |
| `UPLOAD_DIR` | Кодод хатуу бичигдсэн `./uploads` | Орчны хувьсагчаар тохируулагдана ✅ |
| Disk тохиргоо | Байхгүй | `render.yaml`-д бэлэн ✅ |

**Код дээрх өөрчлөлт:** `api/src/storage/uploads.controller.ts`

```ts
export const UPLOAD_DIR =
  process.env.UPLOAD_DIR?.trim() || join(process.cwd(), 'uploads');
```

Локал хөгжүүлэлтэд нөлөөлөхгүй (хувьсагч байхгүй бол `./uploads` руу буцаж унана).

---

## 3️⃣ Яагаад S3 биш вэ?

Эхлээд S3/R2 руу шилжих санал байсан. Гэхдээ шалгахад:

```
grep -rn "S3_\|AWS_\|BUCKET" api/src   →  ҮР ДҮН БАЙХГҮЙ
```

**Кодод S3-ийн хэрэгжилт огт байхгүй** — зөвхөн `multer` + локал диск. S3 руу
шилжих гэвэл шинэ SDK, credential менежмент, upload/serve замыг дахин бичих
шаардлагатай — хэдэн өдрийн ажил.

**Render disk нь код өөрчлөхгүйгээр яг ижил асуудлыг шийднэ.** Хэрэглэгч
олширч, олон instance болох үед л S3 хэрэгтэй болно (disk нь нэг instance-д
холбогддог). Одоогийн 359 сурагчийн хэмжээнд disk бүрэн хангалттай.

> 📌 Ирээдүйд S3 руу шилжихэд `uploads.controller.ts`-ийн дотоод хэрэгжилт л
> солигдоно — `key` буцаах гэрээ хэвээрээ тул бусад код хөндөгдөхгүй.

---

## 4️⃣ Байршуулах дараалал

```bash
# 1. Локал шалгалт
cd api && npx tsc --noEmit && npx jest --silent
cd ../web && npx tsc --noEmit && npx next build
```

```bash
# 2. Migration-ууд production дээр автоматаар ажиллана
#    (startCommand дотор: npx prisma migrate deploy)
```

> ⚠️ **Локал өгөгдлийг production руу хуулах:** 359 сурагч **локал** DB-д
> импортлогдсон (`localhost:5432/pimn`). Production руу зөөх бол:
>
> ```bash
> pg_dump --data-only --no-owner "postgresql://localhost:5432/pimn" > data.sql
> ```
>
> Дараа нь Render-ийн `psql` рүү сэргээнэ. **Эсвэл** илүү энгийнээр:
> `DATABASE_URL`-ыг production руу заагаад импортын скриптийг дахин
> ажиллуулна (идемпотент тул давхардахгүй):
>
> ```bash
> DATABASE_URL="<render-connection-string>" npm run import:students -- --commit
> ```

---

## 5️⃣ Байршуулсны дараа шалгах

- [ ] `https://web-one-pi-59.vercel.app` нээгдэж байна
- [ ] Нэвтрэлт ажиллаж байна (CORS алдаа гарахгүй — `WEB_ORIGIN` зөв)
- [ ] Зураг байршуулаад **redeploy хийж үзээд** зураг үлдсэн эсэхийг шалгах
- [ ] Цайвар/харанхуй горим хоёулаа зөв харагдаж байна
- [ ] Migration-ууд бүгд хэрэглэгдсэн: `npx prisma migrate status`

---

## 6️⃣ Хараахан хийгээгүй (ирээдүйд)

| Юу | Хэзээ хэрэгтэй болох вэ |
|---|---|
| CI (GitHub Actions) | Хэд хэдэн хүн код бичиж эхлэхэд |
| Автомат нөөцлөлт | Төлбөртэй Postgres багц дээр Render өөрөө хийдэг |
| S3/R2 | Олон instance болох үед (одоо шаардлагагүй) |
| Хяналт/alert | Хэрэглэгч 1000+ болоход |
| Тестийн хамрах хүрээ | `grading.spec.ts` (22 тест) л жинхэнэ бий |
