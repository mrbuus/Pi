# Pi.mn — Production байршуулалтын гарын авлага

> Сүүлд шинэчилсэн: 2026-07-28
> Одоогийн топологи: **API + Postgres → Render** · **Web → Vercel**

---

## 🚨 ХАМГИЙН ЯАРАЛТАЙ: өгөгдлийн сан устах хугацаа

Render API-аас уншсан **бодит** утга:

```
pimn-db · dpg-d9ee52jrjlhs73c8sbl0-a · багц: free · төлөв: available
expiresAt: 2026-08-18T14:43:22Z
```

| Огноо | Юу болох вэ |
|---|---|
| **2026-08-18** | Сан **хугацаа дуусч, хаагдана** (хандах боломжгүй) |
| **2026-09-01 орчим** | Render 14 хоногийн дараа **бүрмөсөн УСТГАНА** |

Устсаны дараа **359 сурагчийн бүртгэл, бүх дүн, ирц, даалгавар сэргээх
боломжгүй**. Төлбөртэй багц руу сайжруулбал өгөгдөл бүрэн хадгалагдана
(шинээр үүсгэх шаардлагагүй).

---

## ⚠️ Яг одоо байгаа эрсдэлүүд

| # | Эрсдэл | Үр дагавар | Шийдэл |
|---|---|---|---|
| 1 | **Postgres `free` багц** | **2026-08-18-нд хаагдаж, ~09-01-нд УСТАНА** | Багц сайжруулах (доор) |
| 2 | **Тогтвортой диск байхгүй** | Байршуулсан зураг/PDF **redeploy бүрт устдаг** | `starter` + disk (доор) |
| 3 | **Web сервис `free`** | Идэвхгүй үед унтарч, эхний хүсэлт 30+ секунд болдог | `starter` руу сайжруулах |
| 4 | **Blueprint sync амжилтгүй** | `render.yaml` дэх тохиргоо **огт хэрэгждэггүй** | Доорх §1.5-ыг үз |

> 💡 Эдгээр нь кодын алдаа биш — **багцын хязгаарлалт**. Код бүрэн бэлэн болсон.

---

## 1.5️⃣ Blueprint sync-ийн талаар (2026-07-28-нд илрүүлсэн)

Render-ээс "**Blueprint Sync Failed for pimn**" гэсэн имэйл ирдэг. Шалгасан үр дүн:

```
blueprint: exs-d9ee4cf41pts73ep1fkg · autoSync: true · status: error
sync түүх: 3/3 БҮГД error (анхных нь 2026-07-19)
```

**Чухал ялгаа — sync ба deploy хоёр ТУСДАА:**

| | Юу хийдэг | Одоогийн төлөв |
|---|---|---|
| **Auto-deploy** | git push → build → ажиллуулах | ✅ **Ажиллаж байна** (`ed753cc` live) |
| **Blueprint sync** | `render.yaml` → багц, диск, env var | ❌ Хэзээ ч амжилттай болоогүй |

Тиймээс **код тогтмол шинэчлэгдсээр байсан** — зөвхөн `render.yaml` дэх
тунхаглал хэрэгжээгүй. Сервис нь эхэндээ гараар үүссэн байна.

**Яагаад алдаа гарсан бэ:** `free` багцын web сервист **тогтвортой диск
дэмжигддэггүй** (Render-ийн баримт бичигт free сервисийн хязгаарлалтад
"Persistent disks" гэж шууд заасан). Render API дээр ч `disk: null` —
өөрөөр хэлбэл огт үүсээгүй.

**Хийсэн засвар:** `render.yaml` дээрх `disk:` болон `UPLOAD_DIR`-ыг түр
тайлбар (#) болгосон. Багцаа `starter` болгомогц 2 мөрийн тайлбарыг авахад
буцаад идэвхжинэ (файлд нь заавар бичсэн).

**Одоогоор алдагдал байхгүй:** хэрэгжээгүй `DB_POOL_MAX=15`,
`DB_POOL_CONNECT_TIMEOUT_MS=5000`, `DB_STATEMENT_TIMEOUT_MS=10000` гурав нь
кодын **үндсэн утгатай яг ижил** (`prisma.service.ts`) тул ажиллагаанд
ялгаа гараагүй. `WEB_ORIGIN` нь гараар зөв тохируулагдсан байсан.

---

## 1️⃣ Render багц сайжруулах (таны хийх ганц гар ажил)

Би төлбөрийн үйлдэл хийж чадахгүй тул энэ алхмыг та өөрөө хийнэ. Бусад бүх бэлтгэл дууссан.

### Postgres

1. [dashboard.render.com](https://dashboard.render.com) → **pimn-db**
2. **Settings → Plan → Change Plan**
3. **Basic-256mb** сонгоно (хамгийн хямд төлбөртэй сонголт, устдаггүй)

> ⏰ **Яаралтай:** үнэгүй багц үүсгэснээс хойш 30 хоногт устдаг.
> Устахаас өмнө сайжруулбал өгөгдөл бүрэн хадгалагдана.

### Web сервис + диск

1. **pimn-api** → **Settings → Plan** → **Starter**
2. Дараа нь **Disks → Add Disk**:
   - Name: `pimn-uploads`
   - Mount path: `/var/data/uploads`
   - Size: `5 GB`

3. `render.yaml` дотор **2 блокийн тайлбарын тэмдгийг (#) авна**:
   - `disk:` (4 мөр) — файлд `✅ ДАРААХ 2 АЛХМААР СЭРГЭЭНЭ` гэж тэмдэглэсэн
   - `- key: UPLOAD_DIR` (2 мөр)
4. `plan: free` → `plan: starter` болгоод push хийнэ

> ⚠️ **UPLOAD_DIR-ыг disk-гүйгээр ИДЭВХЖҮҮЛЖ БОЛОХГҮЙ.** Mount хийгээгүй
> `/var/data/uploads` руу бичих эрх байхгүй тул API нь байршуулах санг
> үүсгэж чадахгүй. (Код нь одоо үүнээс болж унахгүй — 503 буцаагаад
> ажилласаар байна — гэхдээ файл байршуулалт огт ажиллахгүй.)

---

## 2️⃣ Аль хэдийн зассан зүйлс ✅

| Юу | Өмнө | Одоо |
|---|---|---|
| `WEB_ORIGIN` | `https://placeholder.vercel.app` ❌ | `https://web-one-pi-59.vercel.app` ✅ |
| `UPLOAD_DIR` | Кодод хатуу бичигдсэн `./uploads` | Орчны хувьсагчаар тохируулагдана ✅ |
| Disk тохиргоо | Байхгүй | `render.yaml`-д бэлэн ✅ |

**Код дээрх өөрчлөлт:** `api/src/storage/uploads.controller.ts`

```ts
export const UPLOAD_DIR =
  process.env.UPLOAD_DIR?.trim() || join(process.cwd(), 'uploads');
```

Локал хөгжүүлэлтэд нөлөөлөхгүй (хувьсагч байхгүй бол `./uploads` руу буцаж унана).

---

## 2.5️⃣ Production-ыг ТЭГ-ЭЭС байгуулах дараалал

⚠️ Сан устаж дахин үүсгэх шаардлага гарвал (§🚨-г үз) энэ дарааллаар сэргээнэ.
Бүх скрипт **идемпотент** тул давхар ажиллуулахад аюулгүй.

```bash
# 0) Схем
DATABASE_URL="<render-ийн-external-url>?sslmode=verify-full" npx prisma migrate deploy

# 1) Ажилтан (админ + багш нар) — жагсаалт: prisma/reports/staff-roster.json
DATABASE_URL="...?sslmode=verify-full" node prisma/upsert-staff.cjs --commit --update-names

# 2) Сурагч + эцэг эх (ангиас 10-аар нь түүвэрлэнэ)
DATABASE_URL="...?sslmode=verify-full" \
  node prisma/import-sample-with-parents.cjs --classes=12-2,11-2,10-1 --commit
```

**Анхаарах зүйлс:**

- **`?sslmode=verify-full` заавал.** Render-ийн гадаад холболт SSL шаарддаг;
  үгүй бол `User was denied access on the database` гэсэн ойлгомжгүй алдаа өгнө.
- **Алсын сан удаан.** Oregon руу нэг бүртгэл үүсгэхэд ~4 round-trip тул 60
  хэрэглэгч ~2 минут болно. Терминалын timeout-д бүү оруул — фонд ажиллуул.
- **🔑 ТҮР НУУЦ ҮГ НЭГ Л УДАА ХЭВЛЭГДЭНЭ.** Санд зөвхөн bcrypt hash үлддэг тул
  алдвал буцааж авах БОЛОМЖГҮЙ — зөвхөн шинэчилж болно. Скриптүүд нууц үгийг
  үүсэх бүрд нь **мөрөөр** хэвлэдэг (төгсгөлд нэг дор биш) — процесс тасарсан ч
  аль хэдийн үүссэн бүртгэлийнх нь гарт үлдэнэ.
- **Бүрэн 359 сурагч** оруулах бол `import-students.cjs --commit`-ыг ашиглана
  (энэ нь түүвэр биш, бүгдийг оруулна).

---

## 3️⃣ Яагаад S3 биш вэ?

Эхлээд S3/R2 руу шилжих санал байсан. Гэхдээ шалгахад:

```
grep -rn "S3_\|AWS_\|BUCKET" api/src   →  ҮР ДҮН БАЙХГҮЙ
```

**Кодод S3-ийн хэрэгжилт огт байхгүй** — зөвхөн `multer` + локал диск. S3 руу
шилжих гэвэл шинэ SDK, credential менежмент, upload/serve замыг дахин бичих
шаардлагатай — хэдэн өдрийн ажил.

**Render disk нь код өөрчлөхгүйгээр яг ижил асуудлыг шийднэ.** Хэрэглэгч
олширч, олон instance болох үед л S3 хэрэгтэй болно (disk нь нэг instance-д
холбогддог). Одоогийн 359 сурагчийн хэмжээнд disk бүрэн хангалттай.

> 📌 Ирээдүйд S3 руу шилжихэд `uploads.controller.ts`-ийн дотоод хэрэгжилт л
> солигдоно — `key` буцаах гэрээ хэвээрээ тул бусад код хөндөгдөхгүй.

---

## 4️⃣ Байршуулах дараалал

```bash
# 1. Локал шалгалт
cd api && npx tsc --noEmit && npx jest --silent
cd ../web && npx tsc --noEmit && npx next build
```

```bash
# 2. Migration-ууд production дээр автоматаар ажиллана
#    (startCommand дотор: npx prisma migrate deploy)
```

> ⚠️ **Локал өгөгдлийг production руу хуулах:** 359 сурагч **локал** DB-д
> импортлогдсон (`localhost:5432/pimn`). Production руу зөөх бол:
>
> ```bash
> pg_dump --data-only --no-owner "postgresql://localhost:5432/pimn" > data.sql
> ```
>
> Дараа нь Render-ийн `psql` рүү сэргээнэ. **Эсвэл** илүү энгийнээр:
> `DATABASE_URL`-ыг production руу заагаад импортын скриптийг дахин
> ажиллуулна (идемпотент тул давхардахгүй):
>
> ```bash
> DATABASE_URL="<render-connection-string>" npm run import:students -- --commit
> ```

---

## 5️⃣ Байршуулсны дараа шалгах

- [ ] `https://web-one-pi-59.vercel.app` нээгдэж байна
- [ ] Нэвтрэлт ажиллаж байна (CORS алдаа гарахгүй — `WEB_ORIGIN` зөв)
- [ ] Зураг байршуулаад **redeploy хийж үзээд** зураг үлдсэн эсэхийг шалгах
- [ ] Цайвар/харанхуй горим хоёулаа зөв харагдаж байна
- [ ] Migration-ууд бүгд хэрэглэгдсэн: `npx prisma migrate status`

---

## 6️⃣ Хараахан хийгээгүй (ирээдүйд)

| Юу | Хэзээ хэрэгтэй болох вэ |
|---|---|
| CI (GitHub Actions) | Хэд хэдэн хүн код бичиж эхлэхэд |
| Автомат нөөцлөлт | Төлбөртэй Postgres багц дээр Render өөрөө хийдэг |
| S3/R2 | Олон instance болох үед (одоо шаардлагагүй) |
| Хяналт/alert | Хэрэглэгч 1000+ болоход |
| Тестийн хамрах хүрээ | `grading.spec.ts` (22 тест) л жинхэнэ бий |
