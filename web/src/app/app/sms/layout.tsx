export const metadata = {
  title: "Дугаарлуу мессеж",
  description: "SMS явуулалтыг удирдах",
};

export default function SmsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
